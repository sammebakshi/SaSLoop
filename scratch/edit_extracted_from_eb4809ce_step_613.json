{
  "Description": "\"Add enableEbillPdf setting, PDF generation helpers (generateReceiptPdfHtml + generatePdfFromHtml), update triggerEbill to support PDF sending, and add 'Send as PDF' checkbox toggle in settings UI.\"",
  "Instruction": "\"1. Add enableEbillPdf to defaultSettings. 2. Replace triggerEbill with PDF-aware version and add PDF helper functions. 3. Add PDF checkbox toggle in settings UI.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":4613,\"ReplacementContent\":\"      enableEbill: false,\\n      ebillMethod: 'DIRECT',\\n      enableEbillPdf: false,\",\"StartLine\":4612,\"TargetContent\":\"      enableEbill: false,\\r\\n      ebillMethod: 'DIRECT',\"},{\"AllowMultiple\":false,\"EndLine\":9067,\"ReplacementContent\":\"  const generateReceiptPdfHtml = (order) => {\\n    const header = (business?.business_name || business?.name || posSettings.receiptHeader || 'SHAHE TEHZEEB RESTAURANT').toUpperCase();\\n    const address = posSettings.address || '';\\n    const phone = posSettings.phone || '';\\n    const gstin = posSettings.gstin || '';\\n    const footer = posSettings.greetingMessage || 'THANK YOU! VISIT AGAIN';\\n    const items = Array.isArray(order.items) ? order.items : JSON.parse(order.items || '[]');\\n    const subtotal = parseFloat(order.subtotal || 0);\\n    const discount = parseFloat(order.discount || 0);\\n    const taxCgst = parseFloat(order.tax_cgst || 0);\\n    const taxSgst = parseFloat(order.tax_sgst || 0);\\n    const deliveryCharge = parseFloat(order.delivery_charge || 0);\\n    const serviceCharge = parseFloat(order.service_charge || 0);\\n    const grandTotal = parseFloat(order.total_price || 0);\\n    const billNo = order.bill_no || order.id;\\n    const dateStr = new Date(order.created_at || Date.now()).toLocaleString();\\n    const paymentMethod = order.payment_method || 'CASH';\\n\\n    let itemsHtml = '';\\n    items.forEach((item, idx) => {\\n      const qty = parseInt(item.qty || 1);\\n      const price = parseFloat(item.price || 0);\\n      const total = qty * price;\\n      itemsHtml += `<tr>\\n        <td style=\\\"padding:6px 8px;border-bottom:1px solid #e2e8f0;font-size:12px;\\\">${idx + 1}</td>\\n        <td style=\\\"padding:6px 8px;border-bottom:1px solid #e2e8f0;font-size:12px;\\\">${item.name}</td>\\n        <td style=\\\"padding:6px 8px;border-bottom:1px solid #e2e8f0;font-size:12px;text-align:center;\\\">${qty}</td>\\n        <td style=\\\"padding:6px 8px;border-bottom:1px solid #e2e8f0;font-size:12px;text-align:right;\\\">Rs ${price\n<truncated 11462 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding PDF E-Bill features to App.jsx\"",
  "toolSummary": "\"Add PDF E-Bill features to App.jsx\""
}