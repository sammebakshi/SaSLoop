{
  "AllowMultiple": "false",
  "Description": "\"Defining passcode action helpers and handleTabClick in App.jsx\"",
  "EndLine": "3840",
  "Instruction": "\"Replace checkReceiptsPermission definition with checkReceiptsPermission plus new passcode helpers and handleTabClick\"",
  "ReplacementContent": "\"     const checkReceiptsPermission = (perm) => {\\n       const access = getStaffPermissions()?.pos_access?.Receipts;\\n       if (!access) return true;\\n       return access[perm] !== false;\\n     };\\n \\n     const verifyPasscodeAction = (moduleName, permissionKey, promptText) => {\\n       const access = getStaffPermissions()?.pos_access;\\n       if (!access) return true;\\n       \\n       let moduleAccess = access[moduleName];\\n       if (moduleName.includes('.')) {\\n         const parts = moduleName.split('.');\\n         moduleAccess = access;\\n         for (const part of parts) {\\n           moduleAccess = moduleAccess?.[part];\\n         }\\n       }\\n       \\n       const passcodeKey = `${permissionKey}_passcode`;\\n       const passcodeRequired = moduleAccess?.[passcodeKey] === true;\\n       \\n       if (!passcodeRequired) {\\n         return true;\\n       }\\n       \\n       const pin = prompt(promptText || `Enter Manager PIN to authorize this action:`);\\n       if (pin === null) return false;\\n       \\n       const staffDataStr = localStorage.getItem('pos_all_staff');\\n       const staff = staffDataStr ? JSON.parse(staffDataStr) : [];\\n       const authorized = staff.find(u => {\\n         const isManager = String(u.role || '').toLowerCase() === 'manager' ||\\n                           String(u.role || '').toLowerCase() === 'admin' ||\\n                           String(u.role || '').toLowerCase() === 'brand_owner' ||\\n                           String(u.designation_name || '').toLowerCase().includes('manager');\\n         return isManager && u.pos_pin && String(u.pos_pin) === String(pin);\\n       });\\n       \\n       if (!authorized) {\\n         toast.error(\\\"Invalid Manager PIN/Passcode!\\\");\\n         return false;\\n       }\\n       \\n       return true;\\n     };\\n \\n     const checkBillingPasscode = (perm, promptText) => {\\n       let modulePath = 'Billing';\\n       if (orderType === 'QUICK') {\\n         modulePath = 'QuickBill';\\n       } else if (orderType === 'DELIVERY') {\\n         modulePath = 'Delivery.Bi\n<truncated 3871 bytes>",
  "StartLine": "3835",
  "TargetContent": "\"     const checkReceiptsPermission = (perm) => {\\n       const access = getStaffPermissions()?.pos_access?.Receipts;\\n       if (!access) return true;\\n       return access[perm] !== false;\\n     };\"",
  "TargetFile": "\"c:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Updating checkReceiptsPermission and adding helpers\"",
  "toolSummary": "\"Adding passcode helper functions in App.jsx\""
}