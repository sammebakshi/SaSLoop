{
  "AllowMultiple": "false",
  "Description": "\"Add includeItemized state and report generation helper methods (compileLedgerData, exportToExcel, exportToPDF) in App.jsx\"",
  "EndLine": "932",
  "Instruction": "\"Insert includeItemized state, compileLedgerData, exportToExcel, and exportToPDF helper functions after refreshCustomerHistory\"",
  "ReplacementContent": "\"  const [historyCustomerPhone, setHistoryCustomerPhone] = useState(null);\\n  const [customerHistoryData, setCustomerHistoryData] = useState({ orders: [], transactions: [] });\\n  const [isHistoryLoading, setIsHistoryLoading] = useState(false);\\n  const [historyTab, setHistoryTab] = useState('orders');\\n  const [includeItemized, setIncludeItemized] = useState(false);\\n\\n  const refreshCustomerHistory = async (phone) => {\\n    if (!phone) return;\\n    try {\\n      const res = await posService.getCustomerHistory(phone);\\n      setCustomerHistoryData(res.data || { orders: [], transactions: [] });\\n    } catch (err) {\\n      console.error(\\\"Failed to refresh customer history:\\\", err);\\n    }\\n  };\\n\\n  const compileLedgerData = () => {\\n    const ledger = [];\\n    if (Array.isArray(customerHistoryData.orders)) {\\n      customerHistoryData.orders.forEach(order => {\\n        const grandTotal = getReceiptGrandTotal(order, posSettings?.isTaxInclusive);\\n        const paid = parseFloat(order.paid_amount || 0);\\n        \\n        let parsedItems = [];\\n        try {\\n          parsedItems = typeof order.items === 'string' ? JSON.parse(order.items) : order.items;\\n          if (!Array.isArray(parsedItems)) parsedItems = [];\\n        } catch (e) {\\n          parsedItems = [];\\n        }\\n        \\n        ledger.push({\\n          date: new Date(order.created_at),\\n          type: 'ORDER',\\n          reference: `#${order.bill_no || order.id}`,\\n          description: `Bill #${order.bill_no || order.id} (${order.payment_method})`,\\n          debit: grandTotal,\\n          credit: paid,\\n          items: parsedItems,\\n          rawRecord: order\\n        });\\n      });\\n    }\\n\\n    if (Array.isArray(customerHistoryData.transactions)) {\\n      customerHistoryData.transactions.forEach(tx => {\\n        const amtVal = parseFloat(tx.amount) || 0;\\n        let debit = 0;\\n        let credit = 0;\\n        if (amtVal > 0) {\\n          credit = amtVal;\\n        } else if (amtVal < 0) {\\n          debit = Math.abs(amtVal);\\n        }\\n    \n<truncated 17590 bytes>",
  "StartLine": "918",
  "TargetContent": "\"  const [historyCustomerPhone, setHistoryCustomerPhone] = useState(null);\\n  const [customerHistoryData, setCustomerHistoryData] = useState({ orders: [], transactions: [] });\\n  const [isHistoryLoading, setIsHistoryLoading] = useState(false);\\n  const [historyTab, setHistoryTab] = useState('orders');\\n\\n  const refreshCustomerHistory = async (phone) => {\\n    if (!phone) return;\\n    try {\\n      const res = await posService.getCustomerHistory(phone);\\n      setCustomerHistoryData(res.data || { orders: [], transactions: [] });\\n    } catch (err) {\\n      console.error(\\\"Failed to refresh customer history:\\\", err);\\n    }\\n  };\\n\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Add helper functions to App.jsx\"",
  "toolSummary": "\"Adding states and report generators to App.jsx\""
}