{
  "AllowMultiple": "false",
  "Description": "\"Remove + prefix from modifiers in customer receipt printout.\"",
  "EndLine": "6374",
  "Instruction": "\"Remove the '+ ' prefix from the modifiers list in the customer receipt print layout.\"",
  "ReplacementContent": "\"                  ${it.modifiers?.length ? it.modifiers.map(m => `\\n                    <div class=\\\"modifier-row\\\">\\n                      <span>${m.name.toUpperCase()} ${(!isComp && parseFloat(m.price || 0) > 0) ? `(Rs ${parseFloat(m.price).toFixed(2)})` : ''}</span>\\n                    </div>\\n                  `).join('') : ''}\"",
  "StartLine": "6369",
  "TargetContent": "\"                  ${it.modifiers?.length ? it.modifiers.map(m => `\\n                    <div class=\\\"modifier-row\\\">\\n                      <span>+ ${m.name.toUpperCase()} ${(!isComp && parseFloat(m.price || 0) > 0) ? `(Rs ${parseFloat(m.price).toFixed(2)})` : ''}</span>\\n                    </div>\\n                  `).join('') : ''}\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Removing + prefix from customer receipt print\"",
  "toolSummary": "\"Edit file\""
}