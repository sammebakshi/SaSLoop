{
  "AllowMultiple": "false",
  "Description": "\"Implement min_redeem_points and max_redeem_per_order limits in the loyalty points row view.\"",
  "EndLine": "10090",
  "Instruction": "\"Incorporate minimum points limits and maximum per order redeem caps.\"",
  "ReplacementContent": "\"                         const activeCart = getActiveCart();\\n                         const { subtotal, total } = calculateTotals(activeCart);\\n                         const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\\n                         \\n                         const minRedeemPoints = parseInt(getLoyaltySetting('min_redeem_points', 0));\\n                         const maxRedeemPerOrder = parseInt(getLoyaltySetting('max_redeem_per_order', 999999));\\n                         const meetsMinThreshold = (trayCustomer.points || 0) >= minRedeemPoints;\\n                         \\n                         const maxRedeemable = meetsMinThreshold\\n                           ? Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio), maxRedeemPerOrder)\\n                           : 0;\\n                         \\n                         let pointsEarned = 0;\\n                         if (getLoyaltySetting('loyalty_enabled', true)) {\\n                           const isDineIn = orderType === 'DINE_IN';\\n                           const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\\n                           const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\\n                           \\n                           let eligible = true;\\n                           if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\\n                           if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\\n                           if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\\n                           \\n                           if (eligible) {\\n                             const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\\n                             const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\\n\n<truncated 3182 bytes>",
  "StartLine": "10027",
  "TargetContent": "\"                         const activeCart = getActiveCart();\\n                         const { subtotal, total } = calculateTotals(activeCart);\\n                         const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\\n                         const maxRedeemable = Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio));\\n                         \\n                         let pointsEarned = 0;\\n                         if (trayFullPhone && !isFreeCheckout) {\\n                           if (getLoyaltySetting('loyalty_enabled', true)) {\\n                             const isDineIn = orderType === 'DINE_IN';\\n                             const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\\n                             const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\\n                             \\n                             let eligible = true;\\n                             if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\\n                             if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\\n                             if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\\n                             \\n                             if (eligible) {\\n                               const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\\n                               const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\\n                               const ratio = pointsAwarded / threshold;\\n                               pointsEarned = Math.floor(total * ratio);\\n                             }\\n                           } else {\\n                             pointsEarned = Math.floor(total / 100);\\n                           }\\n                         }\\n                         \\n     \n<truncated 2666 bytes>",
  "TargetFile": "\"C:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating loyalty row presentation with limits and min requirement checks\"",
  "toolSummary": "\"Update loyalty row view limits\""
}