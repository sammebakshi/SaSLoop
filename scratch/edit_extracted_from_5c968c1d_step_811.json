{
  "AllowMultiple": "false",
  "Description": "\"Filter staff list to load only waiters (designation or role containing waiter)\"",
  "EndLine": "4526",
  "Instruction": "\"Modify the staff list filtering logic to only check if the designation or role includes 'waiter'\"",
  "ReplacementContent": "\"      const waitersOnly = staffList.filter(s => {\\n        const des = String(s.designation_name || '').toLowerCase();\\n        const role = String(s.role || '').toLowerCase();\\n        return des.includes('waiter') || role.includes('waiter');\\n      });\"",
  "StartLine": "4521",
  "TargetContent": "\"      const waitersOnly = staffList.filter(s => {\\n        const des = String(s.designation_name || '').toLowerCase();\\n        const role = String(s.role || '').toLowerCase();\\n        return des.includes('waiter') || des.includes('captain') || des.includes('server') || des.includes('staff') || role.includes('staff') || role.includes('waiter');\\n      });\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating waiter loading filter in App.jsx\"",
  "toolSummary": "\"Edit App.jsx waiter filter\""
}