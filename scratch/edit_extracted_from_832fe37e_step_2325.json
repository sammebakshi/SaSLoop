{
  "AllowMultiple": "false",
  "Description": "\"Extract pointsDiscountAmt and update computedTax to include points discount.\"",
  "EndLine": "7200",
  "Instruction": "\"Modify tax computation to include pointsDiscountAmt.\"",
  "ReplacementContent": "\"    const discountAmt = order.discount !== undefined ? parseFloat(order.discount) : 0;\\n    const pointsDiscountAmt = order.points_discount !== undefined ? parseFloat(order.points_discount) : 0;\\n    const deliveryCharge = order.delivery_charge !== undefined ? parseFloat(order.delivery_charge) : 0;\\n    const serviceCharge = order.service_charge !== undefined ? parseFloat(order.service_charge) : 0;\\n    const tipAmount = order.tip_amount !== undefined ? parseFloat(order.tip_amount) : 0;\\n\\n    const taxRate = parseFloat(posSettings.taxRate || 0);\\n    const isInclusive = posSettings.isTaxInclusive;\\n    let computedTax = 0;\\n    const totalDiscount = discountAmt + pointsDiscountAmt;\\n    if (isInclusive) {\\n      computedTax = (subtotal - totalDiscount) * (taxRate / (100 + taxRate));\\n    } else {\\n      computedTax = (subtotal - totalDiscount) * (taxRate / 100);\\n    }\\n    const tax_cgst = order.tax_cgst !== undefined ? parseFloat(order.tax_cgst) : computedTax / 2;\\n    const tax_sgst = order.tax_sgst !== undefined ? parseFloat(order.tax_sgst) : computedTax / 2;\"",
  "StartLine": "7185",
  "TargetContent": "\"    const discountAmt = order.discount !== undefined ? parseFloat(order.discount) : 0;\\n    const deliveryCharge = order.delivery_charge !== undefined ? parseFloat(order.delivery_charge) : 0;\\n    const serviceCharge = order.service_charge !== undefined ? parseFloat(order.service_charge) : 0;\\n    const tipAmount = order.tip_amount !== undefined ? parseFloat(order.tip_amount) : 0;\\n\\n    const taxRate = parseFloat(posSettings.taxRate || 0);\\n    const isInclusive = posSettings.isTaxInclusive;\\n    let computedTax = 0;\\n    if (isInclusive) {\\n      computedTax = (subtotal - discountAmt) * (taxRate / (100 + taxRate));\\n    } else {\\n      computedTax = (subtotal - discountAmt) * (taxRate / 100);\\n    }\\n    const tax_cgst = order.tax_cgst !== undefined ? parseFloat(order.tax_cgst) : computedTax / 2;\\n    const tax_sgst = order.tax_sgst !== undefined ? parseFloat(order.tax_sgst) : computedTax / 2;\"",
  "TargetFile": "\"C:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Extracting pointsDiscountAmt in handlePrint\"",
  "toolSummary": "\"Extract pointsDiscountAmt\""
}