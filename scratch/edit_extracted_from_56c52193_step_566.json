{
  "ArtifactMetadata": "{\"ArtifactType\":\"task\",\"Summary\":\"Modified fetchOrdersForMode, calculateStats, and fetchDashboardStatsFromServer in App.jsx to disable all server-to-POS downloading of sales records and statistics, ensuring sales are strictly synced one-way from POS to server.\"}",
  "Description": "\"Configured the POS app for strict one-way sales syncing (POS -> Server only). Turned off downloading of individual receipts from the server and disabled dashboard statistics fetching, relying entirely on local operations for client displays.\"",
  "Instruction": "\"Implement strict one-way sales syncing by disabling server-to-pos downloads of orders and stats.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":3060,\"ReplacementContent\":\"  const fetchOrdersForMode = async (mode, start = receiptsStartDate, end = receiptsEndDate) => {\\n    if (!isAuthenticated) return;\\n    setFetchingOrders(true);\\n    try {\\n      // Sales/receipts data is only synced FROM POS TO Server, never loaded from Server to POS.\\n      // We calculate dashboard statistics locally from in-memory recentOrders.\\n      calculateStats(recentOrders);\\n    } catch (err) {\\n      console.error(\\\"Failed to load local stats:\\\", err);\\n    } finally {\\n      setFetchingOrders(false);\\n    }\\n  };\",\"StartLine\":2969,\"TargetContent\":\"  const fetchOrdersForMode = async (mode, start = receiptsStartDate, end = receiptsEndDate) => {\\n    if (!isAuthenticated) return;\\n    setFetchingOrders(true);\\n    try {\\n      const params = {};\\n      if (mode !== 'all') {\\n        params.startDate = formatLocalISO(start);\\n        params.endDate = formatLocalISO(end);\\n      }\\n\\n      const orderRes = await posService.getAllOrders(params);\\n      if (Array.isArray(orderRes.data)) {\\n        const mappedOrders = orderRes.data.map(o => {\\n          const totalPrice = parseFloat(o.total_price || 0);\\n          const discountAmt = parseFloat(o.discount_amount || o.discount || 0);\\n          const cgst = parseFloat(o.tax_cgst || 0);\\n          const sgst = parseFloat(o.tax_sgst || 0);\\n          const tip = parseFloat(o.tip_amount || 0);\\n\\n          const itemsList = Array.isArray(o.items) ? o.items : [];\\n          const calculatedSub = getReceiptSubtotal({ ...o, subtotal: undefined });\\n          const subtotal = calculatedSub > 0 ? calculatedSub : (totalPrice - cgst - sgst + discountAmt - tip);\\n\\n          return {\\n            ...o,\\n            id: o.id,\\n            bill_no: o.bill_no || String(o.id),\\n            customer_name: o.customer_name || 'POS Guest',\\n            customer_phone: o.customer_number || '',\\n            items: itemsList,\\n            subtotal: subtotal,\\n            discount: discountAmt,\\n            discountAmt:\n<truncated 6363 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\""
}