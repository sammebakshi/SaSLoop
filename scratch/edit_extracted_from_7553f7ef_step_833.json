{
  "AllowMultiple": "false",
  "Description": "\"Also clean up temporary tables when all items are transferred out\"",
  "EndLine": "7770",
  "Instruction": "\"Add temp table cleanup when all items are transferred out of a temporary table\"",
  "ReplacementContent": "\"    const activeRemaining = remainingItems.filter(item => !item.isCancelled);\\n    if (activeRemaining.length === 0) {\\n      setTableStatuses(statusPrev => ({ ...statusPrev, [selectedTable.id]: 'AVAILABLE' }));\\n      setTableActiveTimestamps(timePrev => { const n = { ...timePrev }; delete n[selectedTable.id]; return n; });\\n      setTableBillNumbers(numPrev => { const n = { ...numPrev }; delete n[selectedTable.id]; return n; });\\n      releaseTableExtraState(selectedTable.id);\\n      // Remove temporary table from the tables list since all items were transferred\\n      if (selectedTable.is_temporary) {\\n        setTables(prev => prev.filter(t => t.id !== selectedTable.id));\\n      }\\n      setIsOldKOTModalOpen(false);\\n      setSelectedTable(null);\\n    }\"",
  "StartLine": "7762",
  "TargetContent": "\"    const activeRemaining = remainingItems.filter(item => !item.isCancelled);\\n    if (activeRemaining.length === 0) {\\n      setTableStatuses(statusPrev => ({ ...statusPrev, [selectedTable.id]: 'AVAILABLE' }));\\n      setTableActiveTimestamps(timePrev => { const n = { ...timePrev }; delete n[selectedTable.id]; return n; });\\n      setTableBillNumbers(numPrev => { const n = { ...numPrev }; delete n[selectedTable.id]; return n; });\\n      releaseTableExtraState(selectedTable.id);\\n      setIsOldKOTModalOpen(false);\\n      setSelectedTable(null);\\n    }\"",
  "TargetFile": "\"c:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Fix transfer temp table cleanup\"",
  "toolSummary": "\"Fix transfer temp table cleanup\""
}