"                                                 items(sortedItems, key = { it.id }) { item ->\n                                                     val qtyInCart = remember(item.id, cart) {\n                                                         cart.filterKeys { it.id == item.id }.values.sum()\n                                                     }\n                                                     val punchedQty = remember(item.id, oldKotItems) {\n                                                         oldKotItems.filterKeys { it.id == item.id }.values.sum()\n                                                     }\n \n                                                     val resolvedPrice = billingViewModel.resolveItemPrice(item, selectedPriceTier, currentOrderType)\n                                                     val resolvedItem = item.copy(price = resolvedPrice)\n \n                                                     MenuItemCard(\n                                                         item = resolvedItem,\n                                                         qtyInCart = qtyInCart,\n                                                         punchedQty = punchedQty,\n                                                         onAdd = {\n                                                             val hasOpts = optionGroups.any { og -> og.itemId == item.id }\n                                                             if (hasOpts) {\n                                                                 selectedItemForModifiers = resolvedItem\n                                                             } else {\n                                                                 billingViewModel.addToCart(resolvedItem)\n                                                             }\n                                                         },\n                                                         onRemove = { billingViewModel.removeFromCart(resolvedItem) },"