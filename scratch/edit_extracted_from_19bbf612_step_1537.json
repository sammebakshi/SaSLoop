{
  "AllowMultiple": "false",
  "Description": "\"Update fetchOrdersForMode order mapping and merge logic to match and filter duplicates based on ID, order_reference, or reference_no.\"",
  "EndLine": "1872",
  "Instruction": "\"Include order_reference in mappedOrders and upgrade the remoteRefs Set checking in filteredUnsynced mapping.\"",
  "ReplacementContent": "\"            reference_no: o.order_reference || '',\\n            order_reference: o.order_reference || '',\\n            tip_amount: tip,\\n            status: o.status || 'COMPLETED',\\n            table_id: o.table_number || null,\\n            order_type: o.order_type || o.address || 'QUICK',\\n            created_at: o.created_at,\\n            synced: true\\n          };\\n        });\\n        \\n        // Merge with unsynced local orders\\n        const unsyncedLocal = recentOrders.filter(o => o.synced === false || String(o.id).startsWith('L-'));\\n        \\n        // Avoid duplicates if order is now synced\\n        const remoteRefs = new Set();\\n        mappedOrders.forEach(o => {\\n          if (o.order_reference) remoteRefs.add(String(o.order_reference));\\n          if (o.reference_no) remoteRefs.add(String(o.reference_no));\\n          if (o.id) remoteRefs.add(String(o.id));\\n        });\\n        \\n        const filteredUnsynced = unsyncedLocal.filter(o => \\n          !remoteRefs.has(String(o.id)) && \\n          !remoteRefs.has(String(o.order_reference || '')) && \\n          !remoteRefs.has(String(o.reference_no || ''))\\n        );\\n        \\n        const merged = [...filteredUnsynced, ...mappedOrders];\"",
  "StartLine": "1854",
  "TargetContent": "\"            reference_no: o.order_reference || '',\\n            tip_amount: tip,\\n            status: o.status || 'COMPLETED',\\n            table_id: o.table_number || null,\\n            order_type: o.order_type || o.address || 'QUICK',\\n            created_at: o.created_at,\\n            synced: true\\n          };\\n        });\\n        \\n        // Merge with unsynced local orders\\n        const unsyncedLocal = recentOrders.filter(o => o.synced === false || String(o.id).startsWith('L-'));\\n        \\n        // Avoid duplicates if order is now synced\\n        const remoteIds = new Set(mappedOrders.map(o => o.order_reference).filter(Boolean));\\n        const filteredUnsynced = unsyncedLocal.filter(o => !remoteIds.has(o.reference_no));\\n        \\n        const merged = [...filteredUnsynced, ...mappedOrders];\"",
  "TargetFile": "\"c:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Replacing duplicate check merge logic in fetchOrdersForMode\"",
  "toolSummary": "\"Replace file content\""
}