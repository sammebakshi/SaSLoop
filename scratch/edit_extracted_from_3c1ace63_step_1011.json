{
  "AllowMultiple": "false",
  "Description": "\"Fallback to redeemed_points in getReceiptGrandTotal if points_discount or points_redeemed are undefined\"",
  "EndLine": "1281",
  "Instruction": "\"Update pointsDisc calculation to support redeemed_points database fallback\"",
  "ReplacementContent": "\"const getReceiptGrandTotal = (receipt, isTaxInclusive = false) => {\\n  if (!receipt) return 0;\\n  const sub = getReceiptSubtotal(receipt);\\n  const disc = parseFloat(receipt.discountAmt || receipt.discount || 0);\\n  const ptsRedeemed = receipt.points_redeemed !== undefined \\n    ? parseInt(receipt.points_redeemed) \\n    : (receipt.redeemed_points !== undefined ? parseInt(receipt.redeemed_points) : 0);\\n  const pointsDisc = receipt.points_discount !== undefined \\n    ? parseFloat(receipt.points_discount) \\n    : (ptsRedeemed * 0.1);\\n  const couponDisc = parseFloat(receipt.coupon_discount || 0);\\n  const totalDisc = disc + pointsDisc + couponDisc;\\n  const taxC = parseFloat(receipt.tax_cgst || 0);\\n  const taxS = parseFloat(receipt.tax_sgst || 0);\"",
  "StartLine": "1273",
  "TargetContent": "\"const getReceiptGrandTotal = (receipt, isTaxInclusive = false) => {\\n  if (!receipt) return 0;\\n  const sub = getReceiptSubtotal(receipt);\\n  const disc = parseFloat(receipt.discountAmt || receipt.discount || 0);\\n  const pointsDisc = parseFloat(receipt.points_discount || 0);\\n  const couponDisc = parseFloat(receipt.coupon_discount || 0);\\n  const totalDisc = disc + pointsDisc + couponDisc;\\n  const taxC = parseFloat(receipt.tax_cgst || 0);\\n  const taxS = parseFloat(receipt.tax_sgst || 0);\"",
  "TargetFile": "\"C:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Updating getReceiptGrandTotal in App.jsx\"",
  "toolSummary": "\"Update getReceiptGrandTotal\""
}