{
  "AllowMultiple": "false",
  "Description": "\"Add handlePasscodeSubmit function next to checkPermission in App.jsx\"",
  "EndLine": "3161",
  "ReplacementContent": "\"  const checkPermission = (path, callback) => {\\n    const posAccess = business?.staff_permissions?.pos_access;\\n    const role = String(business?.role || '').toLowerCase();\\n    const isSupervisor = role === 'brand_owner' || role === 'master_admin' || role.includes('admin') || role.includes('manager');\\n\\n    if (!posAccess) {\\n      callback();\\n      return;\\n    }\\n\\n    const parts = path.split('.');\\n    let allowed = posAccess;\\n    for (const part of parts) {\\n      allowed = allowed?.[part];\\n    }\\n\\n    if (allowed === false && !isSupervisor) {\\n      toast.error(\\\"Access Denied: You do not have permission to perform this action.\\\");\\n      return;\\n    }\\n\\n    const passcodePath = path + '_passcode';\\n    let needsPasscode = posAccess;\\n    for (const part of passcodePath.split('.')) {\\n      needsPasscode = needsPasscode?.[part];\\n    }\\n\\n    if (needsPasscode === true && !isSupervisor) {\\n      setPasscodeModal({\\n        isOpen: true,\\n        title: `Supervisor Authorization Required`,\\n        requiredPermission: path,\\n        callback: callback,\\n        pin: ''\\n      });\\n      return;\\n    }\\n\\n    callback();\\n  };\\n\\n  const handlePasscodeSubmit = (enteredPin) => {\\n    const supervisor = waitersList.find(s => s.pos_pin === enteredPin && s.status === 'active');\\n    const loggedInMatches = business && business.pos_pin === enteredPin && business.status === 'active';\\n    \\n    let isAllowed = false;\\n    let authUser = null;\\n    \\n    if (supervisor) {\\n      const role = String(supervisor.role || '').toLowerCase();\\n      const isSuper = role === 'brand_owner' || role === 'master_admin' || role.includes('admin') || role.includes('manager');\\n      if (isSuper) {\\n        isAllowed = true;\\n        authUser = supervisor;\\n      } else {\\n        const path = passcodeModal.requiredPermission;\\n        const posAccess = supervisor.staff_permissions?.pos_access;\\n        if (posAccess) {\\n          const parts = path.split('.');\\n          let val = posAccess;\\n          for (const part of\n<truncated 927 bytes>",
  "StartLine": "3120",
  "TargetContent": "\"  const checkPermission = (path, callback) => {\\n    const posAccess = business?.staff_permissions?.pos_access;\\n    const role = String(business?.role || '').toLowerCase();\\n    const isSupervisor = role === 'brand_owner' || role === 'master_admin' || role.includes('admin') || role.includes('manager');\\n\\n    if (!posAccess) {\\n      callback();\\n      return;\\n    }\\n\\n    const parts = path.split('.');\\n    let allowed = posAccess;\\n    for (const part of parts) {\\n      allowed = allowed?.[part];\\n    }\\n\\n    if (allowed === false && !isSupervisor) {\\n      toast.error(\\\"Access Denied: You do not have permission to perform this action.\\\");\\n      return;\\n    }\\n\\n    const passcodePath = path + '_passcode';\\n    let needsPasscode = posAccess;\\n    for (const part of passcodePath.split('.')) {\\n      needsPasscode = needsPasscode?.[part];\\n    }\\n\\n    if (needsPasscode === true && !isSupervisor) {\\n      setPasscodeModal({\\n        isOpen: true,\\n        title: `Supervisor Authorization Required`,\\n        requiredPermission: path,\\n        callback: callback,\\n        pin: ''\\n      });\\n      return;\\n    }\\n\\n    callback();\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding handlePasscodeSubmit to App.jsx\"",
  "toolSummary": "\"Add passcode submit handler\""
}