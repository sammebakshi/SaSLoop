{
  "AllowMultiple": "false",
  "Description": "\"Simplify and correct dashboard chart data fetching and auto-loading hooks\"",
  "EndLine": "2347",
  "Instruction": "\"Refactor getOldestOrderDate, fetchPieData, fetchLineData, fetchBarData and useEffect to be robust and auto-loading\"",
  "ReplacementContent": "\"  const getSevenDaysAgoDate = () => {\\n    const d = new Date();\\n    d.setDate(d.getDate() - 6);\\n    return d.toISOString().split('T')[0];\\n  };\\n\\n  const getOldestOrderDate = (orders) => {\\n    if (!orders || orders.length === 0) return new Date().toISOString().split('T')[0];\\n    const oldest = orders.reduce((acc, o) => {\\n      if (!acc) return o;\\n      if (!o.created_at) return acc;\\n      return new Date(o.created_at) < new Date(acc.created_at) ? o : acc;\\n    }, null);\\n    return oldest && oldest.created_at ? oldest.created_at.split('T')[0] : new Date().toISOString().split('T')[0];\\n  };\\n\\n  const fetchPieData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = pieStartDate || getSevenDaysAgoDate();\\n    const end = pieEndDate || new Date().toISOString().split('T')[0];\\n    \\n    const filtered = recentOrders.filter(o => {\\n      if (o.status === 'CANCELLED' || o.status === 'DELETED') return false;\\n      const date = (o.created_at || o.timestamp || '').split('T')[0];\\n      return date >= start && date <= end;\\n    });\\n\\n    const allItems = filtered.flatMap(o => o.items || []);\\n    const aggregated = allItems.reduce((acc, item) => {\\n      const name = item.product_name || item.name;\\n      const qty = item.quantity || item.qty || 1;\\n      const existing = acc.find(i => i.name === name);\\n      if (existing) {\\n        existing.qty += qty;\\n      } else {\\n        acc.push({ name, qty });\\n      }\\n      return acc;\\n    }, []);\\n\\n    const top = aggregated.sort((a, b) => b.qty - a.qty).slice(0, parseInt(pieLimit) || 5);\\n    setPieItems(top);\\n  };\\n\\n  const fetchLineData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = lineStartDate || getSevenDaysAgoDate();\\n    const end = lineEndDate || new Date().toISOString().split('T')[0];\\n\\n    const daysList = [];\\n    let current = new Date(start);\\n    const last = new Date(end);\\n    while (current <= last) {\\n      daysList.push(current.toISOString().split('T')[0]);\\n\n<truncated 2590 bytes>",
  "StartLine": "2193",
  "TargetContent": "\"  const getOldestOrderDate = (orders) => {\\n    if (!orders || orders.length === 0) return new Date().toISOString().split('T')[0];\\n    const oldest = orders.reduce((acc, o) => {\\n      if (!acc) return o;\\n      if (!o.created_at) return acc;\\n      return new Date(o.created_at) < new Date(acc.created_at) ? o : acc;\\n    }, null);\\n    return oldest && oldest.created_at ? oldest.created_at.split('T')[0] : new Date().toISOString().split('T')[0];\\n  };\\n\\n  const fetchPieData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = pieStartDate || getOldestOrderDate(recentOrders);\\n    const end = pieEndDate || new Date().toISOString().split('T')[0];\\n    \\n    const filtered = recentOrders.filter(o => {\\n      const date = o.created_at?.split('T')[0];\\n      return date >= start && date <= end;\\n    });\\n\\n    const allItems = filtered.flatMap(o => o.items || []);\\n    const aggregated = allItems.reduce((acc, item) => {\\n      const name = item.product_name || item.name;\\n      const qty = item.quantity || item.qty || 1;\\n      const existing = acc.find(i => i.name === name);\\n      if (existing) {\\n        existing.qty += qty;\\n      } else {\\n        acc.push({ name, qty });\\n      }\\n      return acc;\\n    }, []);\\n\\n    const top = aggregated.sort((a, b) => b.qty - a.qty).slice(0, parseInt(pieLimit) || 5);\\n    setPieItems(top);\\n  };\\n\\n  const fetchLineData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = lineStartDate || getOldestOrderDate(recentOrders);\\n    const end = lineEndDate || new Date().toISOString().split('T')[0];\\n\\n    const daysList = [];\\n    let current = new Date(start);\\n    const last = new Date(end);\\n    while (current <= last) {\\n      daysList.push(current.toISOString().split('T')[0]);\\n      current.setDate(current.getDate() + 1);\\n      if (daysList.length > 50) break;\\n    }\\n\\n    const dataPoints = daysList.map(day => {\\n      const dayTotal = recentOrders\\n        .filter(o => o.created_at?.startsWith(da\n<truncated 3643 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Refactoring chart methods\"",
  "toolSummary": "\"App.jsx chart methods refactor edit\""
}