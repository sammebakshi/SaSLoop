{
  "AllowMultiple": "false",
  "Description": "\"Skip background order fetching in silentSync when activeTab is 'digital' to prevent conflicting polls overwriting recentOrders and causing list flickering.\"",
  "EndLine": "3396",
  "Instruction": "\"Update the silentSync useEffect to check if activeTab is not 'digital' before fetching orders, and add activeTab to the dependency array.\"",
  "ReplacementContent": "\"  useEffect(() => {\\n    if (!isAuthenticated) return;\\n\\n    const silentSync = async () => {\\n      // 1. Fetch CRM Customers silently in background\\n      try {\\n        const custRes = await posService.getCustomers();\\n        if (custRes.data && Array.isArray(custRes.data)) {\\n          const customersMap = {};\\n          custRes.data.forEach(c => {\\n            customersMap[c.phone] = {\\n              name: c.name,\\n              phone: c.phone,\\n              address: c.address,\\n              points: Number(c.points) || 0,\\n              orders: Number(c.orders) || 0,\\n              totalSpent: Number(c.total_spent) || 0,\\n              balance: Number(c.balance) || 0\\n            };\\n          });\\n          setCustomerDb(prev => {\\n            if (JSON.stringify(prev) !== JSON.stringify(customersMap)) {\\n              localStorage.setItem('pos_customer_db', JSON.stringify(customersMap));\\n              return customersMap;\\n            }\\n            return prev;\\n          });\\n        }\\n      } catch (e) {\\n        console.warn(\\\"Background sync - Failed to load customers:\\\", e);\\n      }\\n\\n      // 2. Fetch Orders silently in background\\n      try {\\n        if (activeTab !== 'digital') {\\n          await fetchOrdersForMode(receiptsDateMode);\\n        }\\n      } catch (err) {\\n        console.warn(\\\"Background sync - Failed to load orders:\\\", err);\\n      }\\n    };\\n\\n    const syncInterval = setInterval(silentSync, 10000); // Poll every 10 seconds\\n    return () => clearInterval(syncInterval);\\n  }, [isAuthenticated, receiptsDateMode, activeTab]);\"",
  "StartLine": "3354",
  "TargetContent": "\"  useEffect(() => {\\n    if (!isAuthenticated) return;\\n\\n    const silentSync = async () => {\\n      // 1. Fetch CRM Customers silently in background\\n      try {\\n        const custRes = await posService.getCustomers();\\n        if (custRes.data && Array.isArray(custRes.data)) {\\n          const customersMap = {};\\n          custRes.data.forEach(c => {\\n            customersMap[c.phone] = {\\n              name: c.name,\\n              phone: c.phone,\\n              address: c.address,\\n              points: Number(c.points) || 0,\\n              orders: Number(c.orders) || 0,\\n              totalSpent: Number(c.total_spent) || 0,\\n              balance: Number(c.balance) || 0\\n            };\\n          });\\n          setCustomerDb(prev => {\\n            if (JSON.stringify(prev) !== JSON.stringify(customersMap)) {\\n              localStorage.setItem('pos_customer_db', JSON.stringify(customersMap));\\n              return customersMap;\\n            }\\n            return prev;\\n          });\\n        }\\n      } catch (e) {\\n        console.warn(\\\"Background sync - Failed to load customers:\\\", e);\\n      }\\n\\n      // 2. Fetch Orders silently in background\\n      try {\\n        await fetchOrdersForMode(receiptsDateMode);\\n      } catch (err) {\\n        console.warn(\\\"Background sync - Failed to load orders:\\\", err);\\n      }\\n    };\\n\\n    const syncInterval = setInterval(silentSync, 10000); // Poll every 10 seconds\\n    return () => clearInterval(syncInterval);\\n  }, [isAuthenticated, receiptsDateMode]);\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Modifying silentSync in App.jsx\"",
  "toolSummary": "\"Replace file content\""
}