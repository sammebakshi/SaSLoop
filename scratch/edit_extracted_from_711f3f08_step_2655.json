{
  "AllowMultiple": "false",
  "Description": "\"Implement merge-aware table transfer and DB-pending-order syncing inside performTableTransfer.\"",
  "EndLine": "8430",
  "ReplacementContent": "\"  const performTableTransfer = async (targetTable) => {\\n    if (!selectedTable) return;\\n    if (targetTable.id === selectedTable.id) {\\n      toast.error(\\\"Cannot transfer to the same table!\\\");\\n      return;\\n    }\\n\\n    const oldCart = tableCarts[selectedTable.id] || [];\\n    const oldBill = tableBills[selectedTable.id] || [];\\n    const oldStatus = tableStatuses[selectedTable.id] || 'AVAILABLE';\\n    const oldBillNo = tableBillNumbers[selectedTable.id];\\n    const oldTime = tableActiveTimestamps[selectedTable.id];\\n    const oldWaiter = tableWaiters[selectedTable.id] || null;\\n    const oldDiscount = tableDiscounts[selectedTable.id] || { type: 'NONE', value: 0 };\\n    const oldCharges = tableAdditionalCharges[selectedTable.id] || [];\\n    const oldCustomer = tableCustomers[selectedTable.id] || null;\\n\\n    // Check if target table is busy\\n    const isTargetOccupied = (tableBills[targetTable.id] || []).filter(item => !item.isCancelled).length > 0;\\n\\n    let mergedCart = [];\\n    let mergedBill = [];\\n    let targetStatus = oldStatus;\\n    let targetBillNo = oldBillNo || tableBillNumbers[targetTable.id];\\n    let targetTime = oldTime || tableActiveTimestamps[targetTable.id];\\n    let targetWaiter = oldWaiter || tableWaiters[targetTable.id];\\n    let targetDiscount = oldDiscount;\\n    let targetCharges = oldCharges;\\n    let targetCustomer = oldCustomer || tableCustomers[targetTable.id];\\n\\n    if (isTargetOccupied) {\\n      // Merge items\\n      const targetCart = tableCarts[targetTable.id] || [];\\n      const targetBillItems = tableBills[targetTable.id] || [];\\n      mergedCart = mergeBillItems([...targetCart, ...oldCart]);\\n      mergedBill = mergeBillItems([...targetBillItems, ...oldBill]);\\n      targetStatus = tableStatuses[targetTable.id] || 'BILL_SAVED';\\n      targetBillNo = tableBillNumbers[targetTable.id] || oldBillNo;\\n      targetTime = tableActiveTimestamps[targetTable.id] || oldTime;\\n      targetWaiter = tableWaiters[targetTable.id] || oldWaiter;\\n      targetDiscount = tableDiscounts[targe\n<truncated 8153 bytes>",
  "StartLine": "8323",
  "TargetContent": "\"  const performTableTransfer = async (targetTable) => {\\n    if (!selectedTable) return;\\n    if (targetTable.id === selectedTable.id) {\\n      toast.error(\\\"Cannot transfer to the same table!\\\");\\n      return;\\n    }\\n\\n    const oldCart = tableCarts[selectedTable.id] || [];\\n    const oldBill = tableBills[selectedTable.id] || [];\\n    const oldStatus = tableStatuses[selectedTable.id] || 'AVAILABLE';\\n    const oldBillNo = tableBillNumbers[selectedTable.id];\\n    const oldTime = tableActiveTimestamps[selectedTable.id];\\n    const oldWaiter = tableWaiters[selectedTable.id] || null;\\n    const oldDiscount = tableDiscounts[selectedTable.id] || { type: 'NONE', value: 0 };\\n    const oldCharges = tableAdditionalCharges[selectedTable.id] || [];\\n    const oldCustomer = tableCustomers[selectedTable.id] || null;\\n\\n    // Transfer states\\n    setTableCarts(prev => {\\n      const updated = { ...prev };\\n      updated[targetTable.id] = oldCart;\\n      delete updated[selectedTable.id];\\n      return updated;\\n    });\\n\\n    setTableBills(prev => {\\n      const updated = { ...prev };\\n      updated[targetTable.id] = oldBill;\\n      delete updated[selectedTable.id];\\n      return updated;\\n    });\\n\\n    setTableStatuses(prev => {\\n      const updated = { ...prev };\\n      updated[targetTable.id] = oldStatus;\\n      updated[selectedTable.id] = 'AVAILABLE';\\n      return updated;\\n    });\\n\\n    setTableBillNumbers(prev => {\\n      const updated = { ...prev };\\n      if (oldBillNo) {\\n        updated[targetTable.id] = oldBillNo;\\n        delete updated[selectedTable.id];\\n      }\\n      return updated;\\n    });\\n\\n    setTableActiveTimestamps(prev => {\\n      const updated = { ...prev };\\n      if (oldTime) {\\n        updated[targetTable.id] = oldTime;\\n        delete updated[selectedTable.id];\\n      }\\n      return updated;\\n    });\\n\\n    setTableWaiters(prev => {\\n      const updated = { ...prev };\\n      if (oldWaiter) {\\n        updated[targetTable.id] = oldWaiter;\\n      }\\n      delete updated[selectedTable.id];\\n  \n<truncated 1216 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating performTableTransfer implementation\"",
  "toolSummary": "\"Modify App.jsx\""
}