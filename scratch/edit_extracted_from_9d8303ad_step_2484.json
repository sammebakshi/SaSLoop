{
  "AllowMultiple": "false",
  "Description": "\"Filter out duplicate transactions and cancelled orders in compileLedgerData to prevent double counting\"",
  "EndLine": "985",
  "Instruction": "\"Add checks to skip cancelled orders and order-related transactions in compileLedgerData\"",
  "ReplacementContent": "\"  const compileLedgerData = () => {\\n    const ledger = [];\\n    if (Array.isArray(customerHistoryData.orders)) {\\n      customerHistoryData.orders.forEach(order => {\\n        // Skip cancelled orders as they do not affect the balance sheet\\n        if (order.status === 'CANCELLED') return;\\n\\n        const grandTotal = getReceiptGrandTotal(order, posSettings?.isTaxInclusive);\\n        const paid = parseFloat(order.paid_amount || 0);\\n        \\n        let parsedItems = [];\\n        try {\\n          parsedItems = typeof order.items === 'string' ? JSON.parse(order.items) : order.items;\\n          if (!Array.isArray(parsedItems)) parsedItems = [];\\n        } catch (e) {\\n          parsedItems = [];\\n        }\\n        \\n        ledger.push({\\n          date: new Date(order.created_at),\\n          type: 'ORDER',\\n          reference: `#${order.bill_no || order.id}`,\\n          description: `Bill #${order.bill_no || order.id} (${order.payment_method})`,\\n          debit: grandTotal,\\n          credit: paid,\\n          items: parsedItems,\\n          rawRecord: order\\n        });\\n      });\\n    }\\n\\n    if (Array.isArray(customerHistoryData.transactions)) {\\n      customerHistoryData.transactions.forEach(tx => {\\n        // Skip transaction entries that are duplicates of order adjustments\\n        if (['CREDIT_PURCHASE', 'CREDIT_REFUND', 'BILL_PAYMENT_REVERSAL'].includes(tx.type)) {\\n          return;\\n        }\\n        if (tx.type === 'BILL_PAYMENT' && tx.reason && tx.reason.toLowerCase().includes('overpayment')) {\\n          return;\\n        }\\n\\n        const amtVal = parseFloat(tx.amount) || 0;\\n        let debit = 0;\\n        let credit = 0;\\n        if (amtVal > 0) {\\n          credit = amtVal;\\n        } else if (amtVal < 0) {\\n          debit = Math.abs(amtVal);\\n        }\\n        \\n        ledger.push({\\n          date: new Date(tx.created_at),\\n          type: 'TRANSACTION',\\n          reference: tx.type,\\n          description: tx.reason || tx.type.replace('_', ' '),\\n          debit: debit,\\n          c\n<truncated 93 bytes>",
  "StartLine": "934",
  "TargetContent": "\"  const compileLedgerData = () => {\\n    const ledger = [];\\n    if (Array.isArray(customerHistoryData.orders)) {\\n      customerHistoryData.orders.forEach(order => {\\n        const grandTotal = getReceiptGrandTotal(order, posSettings?.isTaxInclusive);\\n        const paid = parseFloat(order.paid_amount || 0);\\n        \\n        let parsedItems = [];\\n        try {\\n          parsedItems = typeof order.items === 'string' ? JSON.parse(order.items) : order.items;\\n          if (!Array.isArray(parsedItems)) parsedItems = [];\\n        } catch (e) {\\n          parsedItems = [];\\n        }\\n        \\n        ledger.push({\\n          date: new Date(order.created_at),\\n          type: 'ORDER',\\n          reference: `#${order.bill_no || order.id}`,\\n          description: `Bill #${order.bill_no || order.id} (${order.payment_method})`,\\n          debit: grandTotal,\\n          credit: paid,\\n          items: parsedItems,\\n          rawRecord: order\\n        });\\n      });\\n    }\\n\\n    if (Array.isArray(customerHistoryData.transactions)) {\\n      customerHistoryData.transactions.forEach(tx => {\\n        const amtVal = parseFloat(tx.amount) || 0;\\n        let debit = 0;\\n        let credit = 0;\\n        if (amtVal > 0) {\\n          credit = amtVal;\\n        } else if (amtVal < 0) {\\n          debit = Math.abs(amtVal);\\n        }\\n        \\n        ledger.push({\\n          date: new Date(tx.created_at),\\n          type: 'TRANSACTION',\\n          reference: tx.type,\\n          description: tx.reason || tx.type.replace('_', ' '),\\n          debit: debit,\\n          credit: credit,\\n          items: [],\\n          rawRecord: tx\\n        });\\n      });\\n    }\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Modify compileLedgerData logic\"",
  "toolSummary": "\"Filtering out duplicate transactions in compileLedgerData\""
}