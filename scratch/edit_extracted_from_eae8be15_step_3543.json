{
  "AllowMultiple": "false",
  "Description": "\"Update filteredOrders sorting based on showOldestFirst, define paginatedOrders helper, add pagination page reset hook, and modify handleToggleSelectAll to use paginatedOrders.\"",
  "EndLine": "929",
  "Instruction": "\"Update filteredOrders sorting logic, paginatedOrders selection, select all visible, and page state resets in App.jsx.\"",
  "ReplacementContent": "\"  const filteredOrders = React.useMemo(() => {\\n    let list = recentOrders;\\n    if (receiptSearchQuery) {\\n      const q = receiptSearchQuery.toLowerCase().trim();\\n      list = recentOrders.filter(o => {\\n        const billNo = String(o.bill_no || o.id || '').toLowerCase();\\n        const customer = String(o.customer_name || '').toLowerCase();\\n        const phone = String(o.customer_phone || o.customer_number || '').toLowerCase();\\n        const table = String(o.table_id || '').toLowerCase();\\n        const type = String(o.order_type || '').toLowerCase();\\n        const status = String(o.status || '').toLowerCase();\\n        const payment = String(o.payment_method || '').toLowerCase();\\n        return (\\n          billNo.includes(q) ||\\n          customer.includes(q) ||\\n          phone.includes(q) ||\\n          table.includes(q) ||\\n          type.includes(q) ||\\n          status.includes(q) ||\\n          payment.includes(q)\\n        );\\n      });\\n    }\\n    \\n    // Sort based on showOldestFirst\\n    return [...list].sort((a, b) => {\\n      const dateA = new Date(a.created_at || a.timestamp || 0);\\n      const dateB = new Date(b.created_at || b.timestamp || 0);\\n      return showOldestFirst ? dateA - dateB : dateB - dateA;\\n    });\\n  }, [recentOrders, receiptSearchQuery, showOldestFirst]);\\n\\n  const receiptsPageSize = 30;\\n  const totalPages = Math.ceil(filteredOrders.length / receiptsPageSize) || 1;\\n\\n  const paginatedOrders = React.useMemo(() => {\\n    const startIdx = (receiptsPage - 1) * receiptsPageSize;\\n    return filteredOrders.slice(startIdx, startIdx + receiptsPageSize);\\n  }, [filteredOrders, receiptsPage]);\\n\\n  // Reset page when search or date filters change\\n  useEffect(() => {\\n    setReceiptsPage(1);\\n  }, [receiptsDateMode, receiptsStartDate, receiptsEndDate, receiptSearchQuery]);\\n\\n  const handleToggleSelectReceipt = (e, orderId) => {\\n    e.stopPropagation();\\n    setSelectedReceiptIds(prev => \\n      prev.includes(orderId) \\n        ? prev.filter(id => id !== orderId) \\n        : \n<truncated 593 bytes>",
  "StartLine": "885",
  "TargetContent": "\"  const filteredOrders = React.useMemo(() => {\\n    if (!receiptSearchQuery) return recentOrders;\\n    const q = receiptSearchQuery.toLowerCase().trim();\\n    return recentOrders.filter(o => {\\n      const billNo = String(o.bill_no || o.id || '').toLowerCase();\\n      const customer = String(o.customer_name || '').toLowerCase();\\n      const phone = String(o.customer_phone || o.customer_number || '').toLowerCase();\\n      const table = String(o.table_id || '').toLowerCase();\\n      const type = String(o.order_type || '').toLowerCase();\\n      const status = String(o.status || '').toLowerCase();\\n      const payment = String(o.payment_method || '').toLowerCase();\\n      return (\\n        billNo.includes(q) ||\\n        customer.includes(q) ||\\n        phone.includes(q) ||\\n        table.includes(q) ||\\n        type.includes(q) ||\\n        status.includes(q) ||\\n        payment.includes(q)\\n      );\\n    });\\n  }, [recentOrders, receiptSearchQuery]);\\n\\n  const handleToggleSelectReceipt = (e, orderId) => {\\n    e.stopPropagation();\\n    setSelectedReceiptIds(prev => \\n      prev.includes(orderId) \\n        ? prev.filter(id => id !== orderId) \\n        : [...prev, orderId]\\n    );\\n  };\\n\\n  const handleToggleSelectAll = () => {\\n    const isAllSelected = filteredOrders.length > 0 && filteredOrders.every(o => selectedReceiptIds.includes(o.id));\\n    if (isAllSelected) {\\n      const visibleIds = new Set(filteredOrders.map(o => o.id));\\n      setSelectedReceiptIds(prev => prev.filter(id => !visibleIds.has(id)));\\n    } else {\\n      const visibleIds = filteredOrders.map(o => o.id);\\n      setSelectedReceiptIds(prev => {\\n        const next = new Set([...prev, ...visibleIds]);\\n        return Array.from(next);\\n      });\\n    }\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating filteredOrders memo in App.jsx\"",
  "toolSummary": "\"Replace file content\""
}