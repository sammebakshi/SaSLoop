{
  "AllowMultiple": "false",
  "Description": "\"Add customerMobileInputRef ref and isSelectingRef flag set on Enter keyboard selection.\"",
  "EndLine": "9646",
  "Instruction": "\"Add ref and selection flag to customer mobile input and enter key keydown handler\"",
  "ReplacementContent": "\"                            <input\\n                              ref={customerMobileInputRef}\\n                              type=\\\"text\\\"\\n                              placeholder=\\\"Customer Mobile\\\"\\n                              value={customerPhone}\\n                              onChange={e => setCustomerPhone(e.target.value)}\\n                              onFocus={() => {\\n                                if (posSettings.showVirtualKeyboard) {\\n                                  setKeyboardTarget({ value: customerPhone, setValue: setCustomerPhone, type: 'number' });\\n                                }\\n                                if (searchResults.length > 0) {\\n                                  setShowSearchResults(true);\\n                                }\\n                              }}\\n                              onKeyDown={(e) => {\\n                                if (showSearchResults && searchResults.length > 0) {\\n                                  if (e.key === 'ArrowDown') {\\n                                    e.preventDefault();\\n                                    setActiveSearchResultIndex(prev =>\\n                                      prev < searchResults.length - 1 ? prev + 1 : prev\\n                                    );\\n                                  } else if (e.key === 'ArrowUp') {\\n                                    e.preventDefault();\\n                                    setActiveSearchResultIndex(prev =>\\n                                      prev > 0 ? prev - 1 : -1\\n                                    );\\n                                  } else if (e.key === 'Enter') {\\n                                    if (activeSearchResultIndex >= 0 && activeSearchResultIndex < searchResults.length) {\\n                                      e.preventDefault();\\n                                      const res = searchResults[activeSearchResultIndex];\\n                                      const parsed = parsePhoneNumber(res.number || '');\\n                                      isSel\n<truncated 617 bytes>",
  "StartLine": "9607",
  "TargetContent": "\"                            <input\\n                              type=\\\"text\\\"\\n                              placeholder=\\\"Customer Mobile\\\"\\n                              value={customerPhone}\\n                              onChange={e => setCustomerPhone(e.target.value)}\\n                              onFocus={() => {\\n                                if (posSettings.showVirtualKeyboard) {\\n                                  setKeyboardTarget({ value: customerPhone, setValue: setCustomerPhone, type: 'number' });\\n                                }\\n                                if (searchResults.length > 0) {\\n                                  setShowSearchResults(true);\\n                                }\\n                              }}\\n                              onKeyDown={(e) => {\\n                                if (showSearchResults && searchResults.length > 0) {\\n                                  if (e.key === 'ArrowDown') {\\n                                    e.preventDefault();\\n                                    setActiveSearchResultIndex(prev =>\\n                                      prev < searchResults.length - 1 ? prev + 1 : prev\\n                                    );\\n                                  } else if (e.key === 'ArrowUp') {\\n                                    e.preventDefault();\\n                                    setActiveSearchResultIndex(prev =>\\n                                      prev > 0 ? prev - 1 : -1\\n                                    );\\n                                  } else if (e.key === 'Enter') {\\n                                    if (activeSearchResultIndex >= 0 && activeSearchResultIndex < searchResults.length) {\\n                                      e.preventDefault();\\n                                      const res = searchResults[activeSearchResultIndex];\\n                                      const parsed = parsePhoneNumber(res.number || '');\\n                                      setCustomerCountryCode(parsed.dialCode);\\n                       \n<truncated 487 bytes>",
  "TargetFile": "\"c:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Modify customer mobile input ref and keyboard selection\"",
  "toolSummary": "\"Edit App.jsx to bind ref and enter handler\""
}