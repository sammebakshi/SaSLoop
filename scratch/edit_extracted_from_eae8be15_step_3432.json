{
  "AllowMultiple": "false",
  "Description": "\"Add filteredOrders useMemo and checkbox toggling helper functions to App.jsx for the Receipts page search and bulk-selection.\"",
  "EndLine": "879",
  "Instruction": "\"Insert the filteredOrders memoized search, handleToggleSelectReceipt and handleToggleSelectAll functions after handleReSyncBills.\"",
  "ReplacementContent": "\"  const handleReSyncBills = async () => {\\n    const unsyncedOrders = recentOrders.filter(o => o.synced === false || String(o.id).startsWith('L-'));\\n    if (unsyncedOrders.length > 0) {\\n      toast.info(`Syncing ${unsyncedOrders.length} offline bills first...`);\\n      await handleSyncBills();\\n    }\\n    \\n    toast.info(\\\"Re-syncing with server logs...\\\");\\n    await fetchOrdersForMode(receiptsDateMode, receiptsStartDate, receiptsEndDate);\\n    toast.success(\\\"Receipt logs fully re-synchronized!\\\");\\n  };\\n\\n  const filteredOrders = React.useMemo(() => {\\n    if (!receiptSearchQuery) return recentOrders;\\n    const q = receiptSearchQuery.toLowerCase().trim();\\n    return recentOrders.filter(o => {\\n      const billNo = String(o.bill_no || o.id || '').toLowerCase();\\n      const customer = String(o.customer_name || '').toLowerCase();\\n      const phone = String(o.customer_phone || o.customer_number || '').toLowerCase();\\n      const table = String(o.table_id || '').toLowerCase();\\n      const type = String(o.order_type || '').toLowerCase();\\n      const status = String(o.status || '').toLowerCase();\\n      const payment = String(o.payment_method || '').toLowerCase();\\n      return (\\n        billNo.includes(q) ||\\n        customer.includes(q) ||\\n        phone.includes(q) ||\\n        table.includes(q) ||\\n        type.includes(q) ||\\n        status.includes(q) ||\\n        payment.includes(q)\\n      );\\n    });\\n  }, [recentOrders, receiptSearchQuery]);\\n\\n  const handleToggleSelectReceipt = (e, orderId) => {\\n    e.stopPropagation();\\n    setSelectedReceiptIds(prev => \\n      prev.includes(orderId) \\n        ? prev.filter(id => id !== orderId) \\n        : [...prev, orderId]\\n    );\\n  };\\n\\n  const handleToggleSelectAll = () => {\\n    const isAllSelected = filteredOrders.length > 0 && filteredOrders.every(o => selectedReceiptIds.includes(o.id));\\n    if (isAllSelected) {\\n      const visibleIds = new Set(filteredOrders.map(o => o.id));\\n      setSelectedReceiptIds(prev => prev.filter(id => !visibleIds.has(id)\n<truncated 229 bytes>",
  "StartLine": "867",
  "TargetContent": "\"  const handleReSyncBills = async () => {\\n    const unsyncedOrders = recentOrders.filter(o => o.synced === false || String(o.id).startsWith('L-'));\\n    if (unsyncedOrders.length > 0) {\\n      toast.info(`Syncing ${unsyncedOrders.length} offline bills first...`);\\n      await handleSyncBills();\\n    }\\n    \\n    toast.info(\\\"Re-syncing with server logs...\\\");\\n    await fetchOrdersForMode(receiptsDateMode, receiptsStartDate, receiptsEndDate);\\n    toast.success(\\\"Receipt logs fully re-synchronized!\\\");\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Inserting search and selection helpers in App.jsx\"",
  "toolSummary": "\"Replace file content\""
}