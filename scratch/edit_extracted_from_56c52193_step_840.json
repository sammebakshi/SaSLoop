{
  "ArtifactMetadata": "{\"ArtifactType\":\"other\",\"RequestFeedback\":false,\"Summary\":\"Fix duplicate bill issue by ensuring that `editingOrder` is set to the saved order object when saving or printing a bill, and is restored appropriately when selecting a table with pending bills. Also clear editingOrder when clicking 'Clear Selection' or when settling a bill.\"}",
  "Description": "\"Solve duplicate bill issue by tracking and updating existing order IDs (editingOrder state) instead of treating saved dine-in bills as new orders.\"",
  "Instruction": "\"Apply changes to set, restore, and clear editingOrder state to prevent bill duplication when settling saved bills.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":7250,\"ReplacementContent\":\"          setRecentOrders(prev => prev.map(o => o.id === newOrder.id ? serverOrder : o));\\n          toast.success(`${type === 'SAVE' ? \\\"Saved\\\" : \\\"Settled\\\"} & Synced!`);\\n          if (type === 'SAVE' || type === 'PRINT') {\\n            setEditingOrder(serverOrder);\\n          }\\n        } else {\\n          setRecentOrders(prev => prev.map(o => o.id === newOrder.id ? { ...o, synced: true } : o));\\n          toast.success(`${type === 'SAVE' ? \\\"Saved\\\" : \\\"Settled\\\"} & Synced!`);\\n          if (type === 'SAVE' || type === 'PRINT') {\\n            setEditingOrder({ ...newOrder, synced: true });\\n          }\\n        }\\n      }\\n\\n      if (type === 'SETTLE' && editingPreOrder) {\\n        try {\\n          await posService.updatePreOrderStatus(editingPreOrder.id, 'COMPLETED');\\n          setPreOrders(prev => prev.map(p => p.id === editingPreOrder.id ? { ...p, status: 'COMPLETED', advance_paid: p.total_price, balance_due: 0 } : p));\\n        } catch (poErr) {\\n          console.error(\\\"Failed to complete pre-order:\\\", poErr);\\n        }\\n      }\\n    } catch (err) {\\n      toast.error(\\\"Offline Mode: Saved Locally Only\\\");\\n      if (type === 'SAVE' || type === 'PRINT') {\\n        setEditingOrder(newOrder);\\n      }\\n    }\",\"StartLine\":7195,\"TargetContent\":\"          setRecentOrders(prev => prev.map(o => o.id === newOrder.id ? serverOrder : o));\\n          toast.success(`${type === 'SAVE' ? \\\"Saved\\\" : \\\"Settled\\\"} & Synced!`);\\n        } else {\\n          setRecentOrders(prev => prev.map(o => o.id === newOrder.id ? { ...o, synced: true } : o));\\n          toast.success(`${type === 'SAVE' ? \\\"Saved\\\" : \\\"Settled\\\"} & Synced!`);\\n        }\\n      }\\n\\n      if (type === 'SETTLE' && editingPreOrder) {\\n        try {\\n          await posService.updatePreOrderStatus(editingPreOrder.id, 'COMPLETED');\\n          setPreOrders(prev => prev.map(p => p.id === editingPreOrder.id ? { ...p, status: 'COMPLETED', advance_paid: p.total_price, balance_due: 0 } : p));\\n   \n<truncated 1967 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Applying multi-replacements to fix bill duplication\"",
  "toolSummary": "\"Fix bill duplication\""
}