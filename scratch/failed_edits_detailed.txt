========================================================================
Edit 57 (Step 2133): "Conditionally load Delivery Boy selector button instead of Waiter button in bottom toolbar when order type is PICKUP/DELIVERY and toggle is set to Delivery."
Tool: replace_file_content
StartLine: 9603, EndLine: 9614
------------------ TARGET ------------------
"                        <button\n                          onClick={() => setIsWaiterModalOpen(true)}\n                          className={`flex items-center gap-1.5 px-2 py-1.5 rounded-full text-[10px] font-bold border transition-colors ${selectedWaiter ? 'bg-emerald-500/10 border-emerald-500/50 text-emerald-600 dark:text-emerald-400' : (isDark ? 'bg-gray-800 border-gray-700 text-gray-300' : 'bg-slate-100 border-slate-200 text-slate-600')}`}\n                        >\n                          <svg viewBox=\"0 0 24 24\" width=\"14\" height=\"14\" stroke=\"currentColor\" strokeWidth=\"2.5\" fill=\"none\">\n                            <path d=\"M3 20h18\" strokeLinecap=\"round\" />\n                            <path d=\"M19 16a7 7 0 0 0-14 0z\" />\n                            <path d=\"M12 5v4M10 5h4\" strokeLinecap=\"round\" />\n                          </svg>\n                          {selectedWaiter ? selectedWaiter.name : 'Select Waiter'}\n                        </button>"
------------------ REPLACEMENT ------------------
"                        {orderType === 'PICKUP' && subOrderType === 'DELIVERY' ? (\n                          <button\n                            onClick={() => setIsRiderModalOpen(true)}\n                            className={`flex items-center gap-1.5 px-2 py-1.5 rounded-full text-[10px] font-bold border transition-colors ${selectedRiderId ? 'bg-emerald-500/10 border-emerald-500/50 text-emerald-600 dark:text-emerald-400' : (isDark ? 'bg-gray-800 border-gray-700 text-gray-300' : 'bg-slate-100 border-slate-200 text-slate-600')}`}\n                          >\n                            <svg viewBox=\"0 0 24 24\" width=\"14\" height=\"14\" stroke=\"currentColor\" strokeWidth=\"2.5\" fill=\"none\">\n                              <path d=\"M3 20h18\" strokeLinecap=\"round\" />\n                              <path d=\"M19 16a7 7 0 0 0-14 0z\" />\n                              <path d=\"M12 5v4M10 5h4\" strokeLinecap=\"round\" />\n                            </svg>\n                            {selectedRiderId && riders.find(r => String(r.id) === String(selectedRiderId)) ? riders.find(r => String(r.id) === String(selectedRiderId)).name : 'Select Delivery Boy'}\n                          </button>\n                        ) : (\n                          <button\n                            onClick={() => setIsWaiterModalOpen(true)}\n                            className={`flex items-center gap-1.5 px-2 py-1.5 rounded-full text-[10px] font-bold border transition-colors ${selectedWaiter ? 'bg-emerald-500/10 border-emerald-500/50 text-emerald-600 dark:text-emerald-400' : (isDark ? 'bg-gray-800 border-gray-700 text-gray-300' : 'bg-slate-100 border-slate-200 text-slate-600')}`}\n                          >\n                            <svg viewBox=\"0 0 24 24\" width=\"14\" height=\"14\" stroke=\"currentColor\" strokeWidth=\"2.5\" fill=\"none\">\n                              <path d=\"M3 20h18\" strokeLinecap=\"round\" />\n                              <path d=\"M19 16a7 7 0 0 0-14 0z\" />\n                           
<truncated 244 bytes>
========================================================================
Edit 58 (Step 2137): "Render Rider Selection Modal markup."
Tool: replace_file_content
StartLine: 16584, EndLine: 16588
------------------ TARGET ------------------
"         )}\n\n         {/* Customer History Modal */}\n         {isCustomerHistoryModalOpen && ("
------------------ REPLACEMENT ------------------
"         )}\n\n         {/* Rider Selection Modal */}\n         {isRiderModalOpen && (\n            <div className=\"fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4\">\n               <div className={`border rounded-2xl w-full max-w-md flex flex-col shadow-2xl ${\n                  isDark ? 'bg-[#0d1117] border-[#30363d] text-[#c9d1d9]' : 'bg-white border-slate-200 text-slate-800'\n               }`}>\n                  {/* Header */}\n                  <div className={`p-4 border-b flex justify-between items-center rounded-t-2xl ${\n                     isDark ? 'bg-[#161b22] border-[#30363d]' : 'bg-slate-50 border-slate-200'\n                  }`}>\n                     <div className=\"flex items-center gap-2\">\n                        <svg viewBox=\"0 0 24 24\" width=\"18\" height=\"18\" stroke=\"currentColor\" strokeWidth=\"2.5\" fill=\"none\" className=\"text-emerald-500\">\n                           <path d=\"M3 20h18\" strokeLinecap=\"round\" />\n                           <path d=\"M19 16a7 7 0 0 0-14 0z\" fill=\"currentColor\" />\n                           <path d=\"M12 5v4M10 5h4\" strokeLinecap=\"round\" />\n                        </svg>\n                        <h3 className=\"text-sm font-bold uppercase tracking-wider\">Select Delivery Boy</h3>\n                     </div>\n                     <button\n                        onClick={() => setIsRiderModalOpen(false)}\n                        className={`hover:opacity-80 text-sm ${isDark ? 'text-[#8b949e]' : 'text-slate-400'}`}\n                     >\n                        ✕\n                     </button>\n                  </div>\n\n                  {/* Content */}\n                  <div className=\"p-4 max-h-[300px] overflow-y-auto flex flex-col gap-2\">\n                     {riders.length === 0 ? (\n                        <div className=\"text-center py-8 text-xs opacity-60\">No riders found. Add riders in Back Office first.</div>\n                     ) : (\n                        riders.map(rider => {\n     
<truncated 3629 bytes>
========================================================================
Edit 81 (Step 2558): "Include total balance and current bill earned points info in the loyalty points row."
Tool: replace_file_content
StartLine: 10016, EndLine: 10035
------------------ TARGET ------------------
"                        const activeCart = getActiveCart();\n                        const { subtotal } = calculateTotals(activeCart);\n                        const pointsRatio = business && business.points_to_amount_ratio !== undefined ? parseFloat(business.points_to_amount_ratio) : 1.00;\n                        const maxRedeemable = Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio));\n                        \n                        return (\n                          <div className={`flex items-center justify-between gap-2 px-2 py-1.5 rounded-lg border mt-1.5 ${\n                            redeemedPoints > 0\n                              ? (isDark ? 'bg-amber-500/5 border-amber-500/20' : 'bg-amber-50 border-amber-200')\n                              : (isDark ? 'bg-slate-500/5 border-slate-700' : 'bg-slate-50 border-slate-200')\n                          }`}>\n                            <div className=\"flex items-center gap-1.5\">\n                              <Gift size={14} className={redeemedPoints > 0 ? 'text-amber-500' : 'text-[#10ac84]'} />\n                              <span className={`text-[10px] font-black ${redeemedPoints > 0 ? 'text-amber-600' : 'text-slate-600 dark:text-gray-300'}`}>\n                                {redeemedPoints > 0 \n                                  ? `Redeemed: ${redeemedPoints} Pts (-${posSettings.currency || 'Rs'} ${(redeemedPoints * pointsRatio).toFixed(2)})`\n                                  : `Loyalty Points: ${trayCustomer.points || 0} Pts`\n                                }\n                              </span>\n                            </div>"
------------------ REPLACEMENT ------------------
"                        const { subtotal, total } = calculateTotals(activeCart);\n                        const pointsRatio = business && business.points_to_amount_ratio !== undefined ? parseFloat(business.points_to_amount_ratio) : 1.00;\n                        const maxRedeemable = Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio));\n                        \n                        let pointsEarned = 0;\n                        if (business && (business.loyalty_enabled ?? true)) {\n                          const isDineIn = orderType === 'DINE_IN';\n                          const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n                          const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n                          \n                          let eligible = true;\n                          if (isDineIn && business.loyalty_points_dinein === false) eligible = false;\n                          if (isPickup && business.loyalty_points_pickup === false) eligible = false;\n                          if (isDelivery && business.loyalty_points_delivery === false) eligible = false;\n                          \n                          if (eligible) {\n                            const threshold = parseFloat(business.loyalty_bill_amount_threshold) || 1000;\n                            const pointsAwarded = parseFloat(business.loyalty_points_earned) || 100;\n                            const ratio = pointsAwarded / threshold;\n                            pointsEarned = Math.floor(total * ratio);\n                          }\n                        }\n                        \n                        return (\n                          <div className={`flex items-center justify-between gap-2 px-2 py-1.5 rounded-lg border mt-1.5 ${\n                            redeemedPoints > 0\n                              ? (isDark ? 'bg-amber-500/5 border-amber-500/20' : 'bg-amber-50 border-amber-200')\n                          
<truncated 989 bytes>
========================================================================
Edit 82 (Step 2562): "Define activeCart in loyalty row calculations to prevent ReferenceError."
Tool: replace_file_content
StartLine: 10015, EndLine: 10017
------------------ TARGET ------------------
"                         const { subtotal, total } = calculateTotals(activeCart);"
------------------ REPLACEMENT ------------------
"                         const activeCart = getActiveCart();\n                         const { subtotal, total } = calculateTotals(activeCart);"
========================================================================
Edit 83 (Step 2598): "Define helper function getLoyaltySetting to retrieve values from business.business_details or business fallback."
Tool: replace_file_content
StartLine: 3382, EndLine: 3388
------------------ TARGET ------------------
"  const [business, setBusiness] = useState(() => {\n    try {\n      const saved = localStorage.getItem('pos_profile');\n      return saved ? JSON.parse(saved) : null;\n    } catch (e) { return null; }\n  });"
------------------ REPLACEMENT ------------------
"  const [business, setBusiness] = useState(() => {\n    try {\n      const saved = localStorage.getItem('pos_profile');\n      return saved ? JSON.parse(saved) : null;\n    } catch (e) { return null; }\n  });\n\n  const getLoyaltySetting = (key, defaultValue) => {\n    if (!business) return defaultValue;\n    if (business.business_details && business.business_details[key] !== undefined) {\n      return business.business_details[key];\n    }\n    if (business[key] !== undefined) {\n      return business[key];\n    }\n    return defaultValue;\n  };"
========================================================================
Edit 86 (Step 2618): "Use getLoyaltySetting helper for loyalty settings in handleCheckout."
Tool: replace_file_content
StartLine: 6605, EndLine: 6627
------------------ TARGET ------------------
"     const pointsRatio = business && business.points_to_amount_ratio !== undefined ? parseFloat(business.points_to_amount_ratio) : 1.00;\n     let pointsEarned = 0;\n     if (fullPhone && !isFreeCheckout) {\n       if (business && (business.loyalty_enabled ?? true)) {\n         const isDineIn = orderType === 'DINE_IN';\n         const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n         const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n         \n         let eligible = true;\n         if (isDineIn && business.loyalty_points_dinein === false) eligible = false;\n         if (isPickup && business.loyalty_points_pickup === false) eligible = false;\n         if (isDelivery && business.loyalty_points_delivery === false) eligible = false;\n         \n         if (eligible) {\n           const threshold = parseFloat(business.loyalty_bill_amount_threshold) || 1000;\n           const pointsAwarded = parseFloat(business.loyalty_points_earned) || 100;\n           const ratio = pointsAwarded / threshold;\n           pointsEarned = Math.floor(total * ratio);\n         }\n       } else {\n         pointsEarned = Math.floor(total / 100);\n       }\n     }"
------------------ REPLACEMENT ------------------
"     const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\n     let pointsEarned = 0;\n     if (fullPhone && !isFreeCheckout) {\n       if (getLoyaltySetting('loyalty_enabled', true)) {\n         const isDineIn = orderType === 'DINE_IN';\n         const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n         const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n         \n         let eligible = true;\n         if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\n         if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\n         if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\n         \n         if (eligible) {\n           const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\n           const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\n           const ratio = pointsAwarded / threshold;\n           pointsEarned = Math.floor(total * ratio);\n         }\n       } else {\n         pointsEarned = Math.floor(total / 100);\n       }\n     }"
========================================================================
Edit 87 (Step 2626): "Implement min_redeem_points and max_redeem_per_order limits in the loyalty points row view."
Tool: replace_file_content
StartLine: 10027, EndLine: 10090
------------------ TARGET ------------------
"                         const activeCart = getActiveCart();\n                         const { subtotal, total } = calculateTotals(activeCart);\n                         const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\n                         const maxRedeemable = Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio));\n                         \n                         let pointsEarned = 0;\n                         if (trayFullPhone && !isFreeCheckout) {\n                           if (getLoyaltySetting('loyalty_enabled', true)) {\n                             const isDineIn = orderType === 'DINE_IN';\n                             const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n                             const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n                             \n                             let eligible = true;\n                             if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\n                             if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\n                             if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\n                             \n                             if (eligible) {\n                               const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\n                               const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\n                               const ratio = pointsAwarded / threshold;\n                               pointsEarned = Math.floor(total * ratio);\n                             }\n                           } else {\n                             pointsEarned = Math.floor(total / 100);\n                           }\n                         }\n                         \n     
<truncated 2666 bytes>
------------------ REPLACEMENT ------------------
"                         const activeCart = getActiveCart();\n                         const { subtotal, total } = calculateTotals(activeCart);\n                         const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\n                         \n                         const minRedeemPoints = parseInt(getLoyaltySetting('min_redeem_points', 0));\n                         const maxRedeemPerOrder = parseInt(getLoyaltySetting('max_redeem_per_order', 999999));\n                         const meetsMinThreshold = (trayCustomer.points || 0) >= minRedeemPoints;\n                         \n                         const maxRedeemable = meetsMinThreshold\n                           ? Math.min(trayCustomer.points || 0, Math.floor(subtotal / pointsRatio), maxRedeemPerOrder)\n                           : 0;\n                         \n                         let pointsEarned = 0;\n                         if (getLoyaltySetting('loyalty_enabled', true)) {\n                           const isDineIn = orderType === 'DINE_IN';\n                           const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n                           const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n                           \n                           let eligible = true;\n                           if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\n                           if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\n                           if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\n                           \n                           if (eligible) {\n                             const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\n                             const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\n
<truncated 3182 bytes>
========================================================================
Edit 91 (Step 2764): "Calculate and include loyalty points properties (points_redeemed, points_discount, points_earned) in handleShowBillPreview order payload so points info shows up in bill preview."
Tool: replace_file_content
StartLine: 6172, EndLine: 6217
------------------ TARGET ------------------
"  const handleShowBillPreview = () => {\n    const activeCart = getActiveCart();\n    if (activeCart.length === 0) {\n      toast.warning(\"Cart is empty!\");\n      return;\n    }\n    const bNo = (selectedTable && tableBillNumbers[selectedTable.id]) ? tableBillNumbers[selectedTable.id] : nextBillNo;\n    const { subtotal, discountAmt, cgst, sgst, serviceCharge, extraFixed, total } = calculateTotals(activeCart);\n    const tempOrder = {\n      id: `PREVIEW-${Date.now()}`,\n      customer_name: customerName || \"POS Guest\",\n      customer_phone: customerPhone,\n      customer_number: customerPhone,\n      items: activeCart.map(i => ({\n        id: i.id,\n        name: i.priceLabel ? `${i.product_name || i.name} (${i.priceLabel})` : (i.product_name || i.name),\n        qty: i.quantity || i.qty || 1,\n        price: i.price,\n        modifiers: i.modifiers || [],\n        kot_category: i.kot_category || \"Main Kitchen\"\n      })),\n      subtotal,\n      discount: discountAmt,\n      tax_cgst: cgst,\n      tax_sgst: sgst,\n      delivery_charge: extraFixed,\n      charge_details: appliedAdditionalCharges.map(c => ({ name: c.name, type: c.type, value: parseFloat(c.value || 0), amount: c.type === 'percent' || c.type === 'PERCENT' ? (subtotal - discountAmt) * (parseFloat(c.value || 0) / 100) : parseFloat(c.value || 0) })),\n      service_charge: serviceCharge,\n      total_price: total,\n      payment_method: 'CASH',\n      reference_no: '',\n      tip_amount: 0,\n      status: 'PREVIEW',\n      table_id: orderType === 'DINE_IN' ? selectedTable?.id : null,\n      order_type: orderType === 'PICKUP' ? subOrderType : orderType,\n      created_at: new Date().toISOString(),\n      timestamp: new Date().toISOString(),\n      bill_no: bNo,\n      waiter_id: selectedWaiter ? selectedWaiter.id : null,\n      waiter_name: selectedWaiter ? selectedWaiter.name : null,\n      rider_id: (orderType === 'PICKUP' && subOrderType === 'DELIVERY' && selectedRiderId) ? parseInt(selectedRiderId) : null,\n      rider_name: (orderType ==
<truncated 338 bytes>
------------------ REPLACEMENT ------------------
"  const handleShowBillPreview = () => {\n    const activeCart = getActiveCart();\n    if (activeCart.length === 0) {\n      toast.warning(\"Cart is empty!\");\n      return;\n    }\n    const bNo = (selectedTable && tableBillNumbers[selectedTable.id]) ? tableBillNumbers[selectedTable.id] : nextBillNo;\n    const { subtotal, discountAmt, cgst, sgst, serviceCharge, extraFixed, total } = calculateTotals(activeCart);\n\n    const pointsRatio = parseFloat(getLoyaltySetting('points_to_amount_ratio', 1.00));\n    let pointsEarned = 0;\n    const fullPhone = customerPhone ? (customerPhone.startsWith('+') ? customerPhone : customerCountryCode + customerPhone) : '';\n    if (fullPhone) {\n      if (getLoyaltySetting('loyalty_enabled', true)) {\n        const isDineIn = orderType === 'DINE_IN';\n        const isPickup = orderType === 'PICKUP' && subOrderType !== 'DELIVERY';\n        const isDelivery = orderType === 'DELIVERY' || (orderType === 'PICKUP' && subOrderType === 'DELIVERY');\n        \n        let eligible = true;\n        if (isDineIn && getLoyaltySetting('loyalty_points_dinein', true) === false) eligible = false;\n        if (isPickup && getLoyaltySetting('loyalty_points_pickup', true) === false) eligible = false;\n        if (isDelivery && getLoyaltySetting('loyalty_points_delivery', true) === false) eligible = false;\n        \n        if (eligible) {\n          const threshold = parseFloat(getLoyaltySetting('loyalty_bill_amount_threshold', 1000));\n          const pointsAwarded = parseFloat(getLoyaltySetting('loyalty_points_earned', 100));\n          const ratio = pointsAwarded / threshold;\n          pointsEarned = total >= threshold ? Math.floor(total * ratio) : 0;\n        }\n      } else {\n        pointsEarned = Math.floor(total / 100);\n      }\n    }\n\n    const tempOrder = {\n      id: `PREVIEW-${Date.now()}`,\n      customer_name: customerName || \"POS Guest\",\n      customer_phone: customerPhone,\n      customer_number: customerPhone,\n      items: activeCart.map(i => ({\n        id: i.id,\n     
<truncated 1845 bytes>
========================================================================
Edit 92 (Step 2770): "Display actual points earned and redeemed in diagnostics HUD loyalty card instead of hardcoded 5% logic."
Tool: replace_file_content
StartLine: 19610, EndLine: 19613
------------------ TARGET ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono\">\n                               Est. Points: {Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                            </span>"
------------------ REPLACEMENT ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono\">\n                               Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                            </span>\n                            {previewReceipt.points_redeemed > 0 && (\n                               <span className=\"text-[9px] text-amber-500 font-bold font-mono\">\n                                  Redeemed: {previewReceipt.points_redeemed} PTS\n                               </span>\n                            )}"
========================================================================
Edit 95 (Step 2782): "Update Est. Points display line in App.jsx."
Tool: replace_file_content
StartLine: 19610, EndLine: 19612
------------------ TARGET ------------------
"                              Est. Points: {Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS"
------------------ REPLACEMENT ------------------
"                              Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                              {previewReceipt.points_redeemed > 0 && (\n                                 <span className=\"text-[9px] text-amber-500 font-bold font-mono\">\n                                    Redeemed: {previewReceipt.points_redeemed} PTS\n                                 </span>\n                              )}"
========================================================================
Edit 96 (Step 2786): "Separate Est. Points and Redeemed Points spans as siblings to maintain clean block structure in preview HUD."
Tool: replace_file_content
StartLine: 19610, EndLine: 19617
------------------ TARGET ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono\">\n                               Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                               {previewReceipt.points_redeemed > 0 && (\n                                  <span className=\"text-[9px] text-amber-500 font-bold font-mono\">\n                                     Redeemed: {previewReceipt.points_redeemed} PTS\n                                  </span>\n                               )}\n                            </span>"
------------------ REPLACEMENT ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono block\">\n                               Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                            </span>\n                            {previewReceipt.points_redeemed > 0 && (\n                               <span className=\"text-[9px] text-amber-500 font-bold font-mono block\">\n                                  Redeemed: {previewReceipt.points_redeemed} PTS\n                               </span>\n                            )}"
========================================================================
Edit 97 (Step 2790): "Separate spans as siblings to maintain clean block structure in preview HUD."
Tool: replace_file_content
StartLine: 19610, EndLine: 19618
------------------ TARGET ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono\">\n                               Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                               {previewReceipt.points_redeemed > 0 && (\n                                  <span className=\"text-[9px] text-amber-500 font-bold font-mono\">\n                                     Redeemed: {previewReceipt.points_redeemed} PTS\n                                  </span>\n                               )}\n                            </span>"
------------------ REPLACEMENT ------------------
"                            <span className=\"text-[9px] text-gray-500 font-mono block\">\n                               Est. Points: {previewReceipt.points_earned !== undefined ? previewReceipt.points_earned : Math.floor((previewReceipt.total_price || 0) * 0.05)} PTS\n                            </span>\n                            {previewReceipt.points_redeemed > 0 && (\n                               <span className=\"text-[9px] text-amber-500 font-bold font-mono block\">\n                                  Redeemed: {previewReceipt.points_redeemed} PTS\n                               </span>\n                            )}"
========================================================================
Edit 98 (Step 2829): "Add Points Discount line to the React preview receipt simulator list."
Tool: replace_file_content
StartLine: 19440, EndLine: 19446
------------------ TARGET ------------------
"                            {parseFloat(previewReceipt.discount || 0) > 0 && (\n                               <div className=\"flex justify-between w-full text-gray-600\">\n                                  <span className=\"text-right flex-1 pr-2\">Discount:</span>\n                                  <span className=\"w-24 text-right\">-{parseFloat(previewReceipt.discount || 0).toFixed(posSettings.decimalPlaces || 2)}</span>\n                               </div>\n                            )}"
------------------ REPLACEMENT ------------------
"                            {parseFloat(previewReceipt.discount || 0) > 0 && (\n                               <div className=\"flex justify-between w-full text-gray-600\">\n                                  <span className=\"text-right flex-1 pr-2\">Discount:</span>\n                                  <span className=\"w-24 text-right\">-{parseFloat(previewReceipt.discount || 0).toFixed(posSettings.decimalPlaces || 2)}</span>\n                               </div>\n                            )}\n                            {parseFloat(previewReceipt.points_discount || 0) > 0 && (\n                               <div className=\"flex justify-between w-full text-gray-600\">\n                                  <span className=\"text-right flex-1 pr-2\">Points Discount:</span>\n                                  <span className=\"w-24 text-right\">-{parseFloat(previewReceipt.points_discount || 0).toFixed(posSettings.decimalPlaces || 2)}</span>\n                               </div>\n                            )}"
