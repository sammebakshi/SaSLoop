{
  "AllowMultiple": "false",
  "Description": "\"Added compileWhatsAppReceipt helper function to App.jsx.\"",
  "EndLine": "7251",
  "Instruction": "\"Add compileWhatsAppReceipt above handleCheckout\"",
  "ReplacementContent": "\"  }, [isPaymentModalOpen]);\\n\\n  const compileWhatsAppReceipt = (order) => {\\n    const businessName = business?.business_name || business?.name || posSettings.receiptHeader || 'SaSLoop POS';\\n    const displayAddress = posSettings.address || '';\\n    const displayPhone = posSettings.phone || '';\\n    const currency = config.currency || '₹';\\n\\n    const orderItems = Array.isArray(order.items) ? order.items : JSON.parse(order.items || '[]');\\n\\n    let itemLines = '';\\n    orderItems.forEach(it => {\\n      const qty = it.qty || it.quantity || 1;\\n      const basePrice = parseFloat(it.price || 0);\\n      const modifiersTotal = (it.modifiers || []).reduce((mAcc, m) => mAcc + parseFloat(m.price || 0), 0);\\n      const itemTotal = qty * (basePrice + modifiersTotal);\\n      \\n      itemLines += `• ${qty}x ${it.name || it.product_name} - ${currency}${itemTotal.toFixed(2)}\\\\n`;\\n      if (it.modifiers && it.modifiers.length > 0) {\\n        it.modifiers.forEach(m => {\\n          itemLines += `  └ ${m.name}\\\\n`;\\n        });\\n      }\\n    });\\n\\n    const subtotal = parseFloat(order.subtotal || 0);\\n    const discount = parseFloat(order.discount || 0);\\n    const tax = (parseFloat(order.tax_cgst || 0) + parseFloat(order.tax_sgst || 0));\\n    const serviceCharge = parseFloat(order.service_charge || 0);\\n    const deliveryCharge = parseFloat(order.delivery_charge || 0);\\n    const grandTotal = parseFloat(order.total_price || 0);\\n\\n    let financialDetails = `*Subtotal:* ${currency}${subtotal.toFixed(2)}\\\\n`;\\n    if (discount > 0) {\\n      financialDetails += `*Discount:* -${currency}${discount.toFixed(2)}\\\\n`;\\n    }\\n    if (tax > 0) {\\n      financialDetails += `*Tax:* ${currency}${tax.toFixed(2)}\\\\n`;\\n    }\\n    if (serviceCharge > 0) {\\n      financialDetails += `*Service Charge:* ${currency}${serviceCharge.toFixed(2)}\\\\n`;\\n    }\\n    if (deliveryCharge > 0) {\\n      financialDetails += `*Delivery/Packing:* ${currency}${deliveryCharge.toFixed(2)}\\\\n`;\\n    }\\n    financialDetails += `*Total:* *${currency}${gr\n<truncated 1044 bytes>",
  "StartLine": "7246",
  "TargetContent": "\"    }\\n  }, [isPaymentModalOpen]);\\n\\n  const handleCheckout = async (type = 'SETTLE', method = 'CASH', referenceNo = '', tip = 0, isDue = false) => {\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding compileWhatsAppReceipt function\"",
  "toolSummary": "\"Modify App.jsx\""
}