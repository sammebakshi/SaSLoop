{
  "AllowMultiple": "false",
  "Description": "\"Update Payment Modal UI in App.jsx to support split payments.\"",
  "EndLine": "10722",
  "Instruction": "\"Update lines 10450 to 10722 in App.jsx to render split payments checkboxes, filtered payment mode options, split amount inputs, sync handler functions, and settle bill call.\"",
  "ReplacementContent": "\"            const numericTip = isFreeMode ? 0 : (parseFloat(tipAmount) || 0);\\n            const grandTotal = calculatedTotal + numericTip;\\n            \\n            let advancePaid = 0;\\n            let netAmountToPayToday = grandTotal;\\n            if (editingPreOrder) {\\n              advancePaid = parseFloat(editingPreOrder.advance_paid) || 0;\\n              netAmountToPayToday = Math.max(0, grandTotal - advancePaid);\\n            }\\n            \\n            const numericPaid = parseFloat(customerPaidAmount) || 0;\\n            const returnAmount = numericPaid > netAmountToPayToday ? numericPaid - netAmountToPayToday : 0;\\n\\n            const handleSplitPaidChange = (val) => {\\n              setSplitPaidAmount(val);\\n              const numVal = parseFloat(val) || 0;\\n              const remaining = Math.max(0, netAmountToPayToday - numVal);\\n              setSplitCreditAmount(remaining.toFixed(2));\\n            };\\n\\n            const handleSplitCreditChange = (val) => {\\n              setSplitCreditAmount(val);\\n              const numVal = parseFloat(val) || 0;\\n              const remaining = Math.max(0, netAmountToPayToday - numVal);\\n              setSplitPaidAmount(remaining.toFixed(2));\\n            };\\n\\n            const filteredOptions = isSplitPayment \\n              ? activeOptions.filter(opt => opt.name.toLowerCase() !== 'credit' && opt.name.toLowerCase() !== 'free')\\n              : activeOptions;\\n\\n            return (\\n               <div className=\\\"fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4\\\">\\n                  <motion.div initial={{ scale: 0.95, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} className={`rounded-3xl border w-full max-w-2xl overflow-hidden shadow-2xl flex flex-col ${isDark ? 'bg-[#0d1117] border-[#30363d] text-white' : 'bg-white border-slate-200 text-slate-900'}`}>\\n                     \\n                     {/* Header */}\\n                     <div className={`p-6 border-b flex justify-between items-center ${isDark ? 'border-[#30363d] bg-[#16\n<truncated 22867 bytes>",
  "StartLine": "10450",
  "TargetContent": "\"            const numericTip = isFreeMode ? 0 : (parseFloat(tipAmount) || 0);\\n            const grandTotal = calculatedTotal + numericTip;\\n            \\n            let advancePaid = 0;\\n            let netAmountToPayToday = grandTotal;\\n            if (editingPreOrder) {\\n              advancePaid = parseFloat(editingPreOrder.advance_paid) || 0;\\n              netAmountToPayToday = Math.max(0, grandTotal - advancePaid);\\n            }\\n            \\n            const numericPaid = parseFloat(customerPaidAmount) || 0;\\n            const returnAmount = numericPaid > netAmountToPayToday ? numericPaid - netAmountToPayToday : 0;\\n\\n            return (\\n               <div className=\\\"fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4\\\">\\n                  <motion.div initial={{ scale: 0.95, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} className={`rounded-3xl border w-full max-w-2xl overflow-hidden shadow-2xl flex flex-col ${isDark ? 'bg-[#0d1117] border-[#30363d] text-white' : 'bg-white border-slate-200 text-slate-900'}`}>\\n                     \\n                     {/* Header */}\\n                     <div className={`p-6 border-b flex justify-between items-center ${isDark ? 'border-[#30363d] bg-[#161b22]' : 'border-slate-200 bg-slate-50'}`}>\\n                        <div>\\n                           <h3 className=\\\"text-lg font-black uppercase italic tracking-tighter flex items-center gap-2\\\">\\n                              Choose Payment Mode\\n                           </h3>\\n                           <p className=\\\"text-[10px] font-bold text-slate-400 uppercase mt-0.5\\\">Bill No: {billNo}</p>\\n                        </div>\\n                        <button onClick={() => setIsPaymentModalOpen(false)} className=\\\"text-slate-400 hover:text-slate-200 text-sm\\\">✕</button>\\n                     </div>\\n\\n                     {/* Content */}\\n                     <div className=\\\"p-6 space-y-6 flex-1 overflow-y-auto no-scrollbar\\\">\\n                        {/* Options Row */}\\n             \n<truncated 18835 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\""
}