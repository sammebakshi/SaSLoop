{
  "AllowMultiple": "false",
  "Description": "\"Implement chart data fetching, formatting, and initial state ranges for Pie, Line, and Bar charts in App.jsx\"",
  "EndLine": "2427",
  "Instruction": "\"Update formatDateLabel, fetchPieData, fetchLineData, fetchBarData, and defaults useEffect in App.jsx.\"",
  "ReplacementContent": "\"  const formatDateLabel = (dateStr, isMonth = false) => {\\n    if (!dateStr) return '';\\n    const d = new Date(dateStr);\\n    if (isNaN(d.getTime())) return dateStr;\\n    const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];\\n    if (isMonth) {\\n      return `${months[d.getMonth()]} ${d.getFullYear().toString().substring(2)}`;\\n    }\\n    const day = d.getDate();\\n    return `${day} ${months[d.getMonth()]}`;\\n  };\\n\\n  const getOldestOrderDate = (orders) => {\\n    if (!orders || orders.length === 0) return new Date().toISOString().split('T')[0];\\n    const oldest = orders.reduce((acc, o) => {\\n      if (!acc) return o;\\n      if (!o.created_at) return acc;\\n      return new Date(o.created_at) < new Date(acc.created_at) ? o : acc;\\n    }, null);\\n    return oldest && oldest.created_at ? oldest.created_at.split('T')[0] : new Date().toISOString().split('T')[0];\\n  };\\n\\n  const fetchPieData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = pieStartDate || new Date().toISOString().split('T')[0];\\n    const end = pieEndDate || new Date().toISOString().split('T')[0];\\n    \\n    const filtered = recentOrders.filter(o => {\\n      if (o.status === 'CANCELLED' || o.status === 'DELETED') return false;\\n      const date = (o.created_at || o.timestamp || '').split('T')[0];\\n      return date >= start && date <= end;\\n    });\\n\\n    const allItems = filtered.flatMap(o => o.items || []);\\n    const aggregated = allItems.reduce((acc, item) => {\\n      const name = item.product_name || item.name;\\n      const qty = item.quantity || item.qty || 1;\\n      const existing = acc.find(i => i.name === name);\\n      if (existing) {\\n        existing.qty += qty;\\n      } else {\\n        acc.push({ name, qty });\\n      }\\n      return acc;\\n    }, []);\\n\\n    const top = aggregated.sort((a, b) => b.qty - a.qty).slice(0, parseInt(pieLimit) || 5);\\n    setPieItems(top);\\n  };\\n\\n  const fetchLineData = () => {\\n    if (!recentOrders || recentOrders.lengt\n<truncated 5187 bytes>",
  "StartLine": "2299",
  "TargetContent": "\"  const formatDateLabel = (dateStr) => {\\n    if (!dateStr) return '';\\n    const d = new Date(dateStr);\\n    if (isNaN(d.getTime())) return dateStr;\\n    const day = d.getDate();\\n    const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];\\n    return `${day} ${months[d.getMonth()]}`;\\n  };\\n\\n  const getOldestOrderDate = (orders) => {\\n    if (!orders || orders.length === 0) return new Date().toISOString().split('T')[0];\\n    const oldest = orders.reduce((acc, o) => {\\n      if (!acc) return o;\\n      if (!o.created_at) return acc;\\n      return new Date(o.created_at) < new Date(acc.created_at) ? o : acc;\\n    }, null);\\n    return oldest && oldest.created_at ? oldest.created_at.split('T')[0] : new Date().toISOString().split('T')[0];\\n  };\\n\\n  const fetchPieData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = pieStartDate || getSevenDaysAgoDate();\\n    const end = pieEndDate || new Date().toISOString().split('T')[0];\\n    \\n    const filtered = recentOrders.filter(o => {\\n      if (o.status === 'CANCELLED' || o.status === 'DELETED') return false;\\n      const date = (o.created_at || o.timestamp || '').split('T')[0];\\n      return date >= start && date <= end;\\n    });\\n\\n    const allItems = filtered.flatMap(o => o.items || []);\\n    const aggregated = allItems.reduce((acc, item) => {\\n      const name = item.product_name || item.name;\\n      const qty = item.quantity || item.qty || 1;\\n      const existing = acc.find(i => i.name === name);\\n      if (existing) {\\n        existing.qty += qty;\\n      } else {\\n        acc.push({ name, qty });\\n      }\\n      return acc;\\n    }, []);\\n\\n    const top = aggregated.sort((a, b) => b.qty - a.qty).slice(0, parseInt(pieLimit) || 5);\\n    setPieItems(top);\\n  };\\n\\n  const fetchLineData = () => {\\n    if (!recentOrders || recentOrders.length === 0) return;\\n    const start = lineStartDate || getSevenDaysAgoDate();\\n    const end = lineEndDate || new Date().toISOString().split('T')[0\n<truncated 2781 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating chart settings and methods\"",
  "toolSummary": "\"Update chart settings and methods\""
}