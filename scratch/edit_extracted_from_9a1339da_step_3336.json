{
  "AllowMultiple": "false",
  "Description": "\"Sync business settings in the background every 10 seconds and instantly when the settings modal is opened in Windows POS.\"",
  "EndLine": "4124",
  "Instruction": "\"Add profile syncing to silentSync background interval, and add a useEffect to sync settings when settings modal is opened.\"",
  "ReplacementContent": "\"  useEffect(() => {\\n    if (!isAuthenticated) return;\\n\\n    const silentSync = async () => {\\n      // 1. Fetch CRM Customers silently in background\\n      try {\\n        const custRes = await posService.getCustomers();\\n        if (custRes.data && Array.isArray(custRes.data)) {\\n          const customersMap = {};\\n          custRes.data.forEach(c => {\\n            customersMap[c.phone] = {\\n              name: c.name,\\n              phone: c.phone,\\n              address: c.address,\\n              points: Number(c.points) || 0,\\n              orders: Number(c.orders) || 0,\\n              totalSpent: Number(c.total_spent) || 0,\\n              balance: Number(c.balance) || 0\\n            };\\n          });\\n          setCustomerDb(prev => {\\n            if (JSON.stringify(prev) !== JSON.stringify(customersMap)) {\\n              localStorage.setItem('pos_customer_db', JSON.stringify(customersMap));\\n              return customersMap;\\n            }\\n            return prev;\\n          });\\n        }\\n      } catch (e) {\\n        console.warn(\\\"Background sync - Failed to load customers:\\\", e);\\n      }\\n\\n      // 2. Fetch Business Profile silently in background to sync central settings (e.g. print_upi_qr)\\n      try {\\n        const profile = await authService.getProfile();\\n        if (profile && profile.data) {\\n          setBusiness(prev => {\\n            if (JSON.stringify(prev) !== JSON.stringify(profile.data)) {\\n              localStorage.setItem('pos_profile', JSON.stringify(profile.data));\\n              return profile.data;\\n            }\\n            return prev;\\n          });\\n        }\\n      } catch (e) {\\n        console.warn(\\\"Background sync - Failed to load profile:\\\", e);\\n      }\\n    };\\n\\n    const syncInterval = setInterval(silentSync, 10000); // Poll every 10 seconds\\n    return () => clearInterval(syncInterval);\\n  }, [isAuthenticated]);\\n\\n  // Sync settings when the Settings modal is opened\\n  useEffect(() => {\\n    if (isSettingsModalOpen && isAuthenticated) {\\n      const fetchFresh\n<truncated 636 bytes>",
  "StartLine": "4088",
  "TargetContent": "\"  useEffect(() => {\\n    if (!isAuthenticated) return;\\n\\n    const silentSync = async () => {\\n      // 1. Fetch CRM Customers silently in background\\n      try {\\n        const custRes = await posService.getCustomers();\\n        if (custRes.data && Array.isArray(custRes.data)) {\\n          const customersMap = {};\\n          custRes.data.forEach(c => {\\n            customersMap[c.phone] = {\\n              name: c.name,\\n              phone: c.phone,\\n              address: c.address,\\n              points: Number(c.points) || 0,\\n              orders: Number(c.orders) || 0,\\n              totalSpent: Number(c.total_spent) || 0,\\n              balance: Number(c.balance) || 0\\n            };\\n          });\\n          setCustomerDb(prev => {\\n            if (JSON.stringify(prev) !== JSON.stringify(customersMap)) {\\n              localStorage.setItem('pos_customer_db', JSON.stringify(customersMap));\\n              return customersMap;\\n            }\\n            return prev;\\n          });\\n        }\\n      } catch (e) {\\n        console.warn(\\\"Background sync - Failed to load customers:\\\", e);\\n      }\\n    };\\n\\n    const syncInterval = setInterval(silentSync, 10000); // Poll every 10 seconds\\n    return () => clearInterval(syncInterval);\\n  }, [isAuthenticated]);\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding background settings syncing and settings modal fetch\"",
  "toolSummary": "\"Update App.jsx settings sync\""
}