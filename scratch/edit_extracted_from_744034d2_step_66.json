{
  "Description": "\"Change POS settings to support local toggle and selection of print payment QR codes on bills, decoupling from backoffice settings toggle.\"",
  "Instruction": "\"Replace references to business settings check for printing payment QR with the local posSettings.printUpiQr toggle and active static QR ID. Filter the dropdown list to only active backend QR codes. Auto-select the first active QR if none is selected.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":3070,\"ReplacementContent\":\"  useEffect(() => {\\n    const printEnabled = !!posSettings.printUpiQr;\\n    if (!previewReceipt || !printEnabled) {\\n      setPreviewQrUrl(null);\\n      setPreviewQrMeta(null);\\n      return;\\n    }\\n    const payeeName = business?.business_name || business?.name || posSettings.receiptHeader || 'SHAHE TEHZEEB RESTAURANT';\\n    const activeEntry = (backendQrs || []).find(e => String(e.id) === String(posSettings.activeStaticUpiId));\\n\\n    if (!activeEntry) {\\n      setPreviewQrUrl(null);\\n      setPreviewQrMeta(null);\\n      return;\\n    }\\n\\n    let upiUri = \\\"\\\";\\n    const isUrl = activeEntry.upi_id.startsWith('http://') || activeEntry.upi_id.startsWith('https://') || activeEntry.upi_id.startsWith('upi://');\\n    if (isUrl) {\\n      upiUri = activeEntry.upi_id;\\n    } else if (posSettings.qrMode === 'dynamic') {\\n      const upiAmount = parseFloat(previewReceipt.total_price || 0).toFixed(2);\\n      upiUri = `upi://pay?pa=${activeEntry.upi_id}&pn=${encodeURIComponent(payeeName)}&am=${upiAmount}&cu=INR`;\\n    } else {\\n      upiUri = `upi://pay?pa=${activeEntry.upi_id}&pn=${encodeURIComponent(payeeName)}&cu=INR`;\\n    }\\n\\n    QRCode.toDataURL(upiUri, { margin: 1, width: 140 })\\n      .then(url => {\\n        setPreviewQrUrl(url);\\n        setPreviewQrMeta({ upiId: activeEntry.upi_id, brand: activeEntry.brand, name: activeEntry.name });\\n      })\\n      .catch(err => console.error('Failed to generate preview QR:', err));\\n  }, [previewReceipt, posSettings.qrMode, posSettings.activeStaticUpiId, posSettings.printUpiQr, backendQrs, business]);\\n\\n  // Auto-select first active QR if none is currently selected in POS settings\\n  useEffect(() => {\\n    if (backendQrs && backendQrs.length > 0) {\\n      const activeExists = backendQrs.some(e => String(e.id) === String(posSettings.activeStaticUpiId));\\n      if (!activeExists) {\\n        const firstActive = backendQrs.find(e => e.is_active) || backendQrs[0];\\n        if (firstActive) {\\n          setPosSettings(\n<truncated 7394 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\""
}