{
  "AllowMultiple": "false",
  "Description": "\"Update receipt print layout to render Google Review QR or Payment QR code mutually exclusively\"",
  "EndLine": "6591",
  "Instruction": "\"Update the qrCodeHtml block to support posSettings.printReviewQr and posSettings.googleReviewUrl in addition to the existing UPI QR logic\"",
  "ReplacementContent": "\"    let qrCodeHtml = '';\\n    if (posSettings.printReviewQr && posSettings.googleReviewUrl) {\\n      try {\\n        const qrDataUrl = await QRCode.toDataURL(posSettings.googleReviewUrl, { margin: 1, width: 140 });\\n        qrCodeHtml = `\\n          <div class=\\\"dashed-line\\\"></div>\\n          <div class=\\\"center\\\" style=\\\"margin: 2mm 0;\\\">\\n             <div style=\\\"font-size: 11.5px; font-weight: bold; margin-bottom: 1mm;\\\">SCAN TO REVIEW</div>\\n             <img src=\\\"${qrDataUrl}\\\" style=\\\"width: 130px; height: 130px; display: block; margin: 0 auto;\\\" />\\n          </div>\\n        `;\\n      } catch (err) {\\n        console.error('Failed to generate Google Review QR Code:', err);\\n      }\\n    } else {\\n      const printEnabled = !!business?.business_details?.settings?.print_upi_qr;\\n      if (printEnabled && posSettings.printUpiQr) {\\n        try {\\n          const payeeName = business?.business_name || business?.name || posSettings.receiptHeader || 'SHAHE TEHZEEB RESTAURANT';\\n          let upiUri = '';\\n          let displayUpiId = '';\\n          let brandLabel = '';\\n\\n          const activeEntry = (backendQrs || []).find(e => String(e.id) === String(posSettings.activeStaticUpiId));\\n          if (activeEntry) {\\n            displayUpiId = activeEntry.upi_id;\\n            brandLabel = activeEntry.name || '';\\n\\n            const isUrl = activeEntry.upi_id.startsWith('http://') || activeEntry.upi_id.startsWith('https://') || activeEntry.upi_id.startsWith('upi://');\\n            if (isUrl) {\\n              upiUri = activeEntry.upi_id;\\n            } else if (posSettings.qrMode === 'dynamic') {\\n              let upiAmount = 0;\\n              if (isBookingReceipt) {\\n                upiAmount = parseFloat(advancePaid);\\n              } else if (isSettlement) {\\n                upiAmount = parseFloat(remainingBalance);\\n              } else {\\n                upiAmount = parseFloat(order.total_price || 0);\\n              }\\n              upiUri = `upi://pay?pa=${activeEntry.upi_id}&pn=${encodeURIComponent(paye\n<truncated 889 bytes>",
  "StartLine": "6544",
  "TargetContent": "\"    let qrCodeHtml = '';\\n    const printEnabled = !!business?.business_details?.settings?.print_upi_qr;\\n    if (printEnabled) {\\n      try {\\n        const payeeName = business?.business_name || business?.name || posSettings.receiptHeader || 'SHAHE TEHZEEB RESTAURANT';\\n        let upiUri = '';\\n        let displayUpiId = '';\\n        let brandLabel = '';\\n\\n        const activeEntry = (backendQrs || []).find(e => String(e.id) === String(posSettings.activeStaticUpiId));\\n        if (activeEntry) {\\n          displayUpiId = activeEntry.upi_id;\\n          brandLabel = activeEntry.name || '';\\n\\n          const isUrl = activeEntry.upi_id.startsWith('http://') || activeEntry.upi_id.startsWith('https://') || activeEntry.upi_id.startsWith('upi://');\\n          if (isUrl) {\\n            upiUri = activeEntry.upi_id;\\n          } else if (posSettings.qrMode === 'dynamic') {\\n            let upiAmount = 0;\\n            if (isBookingReceipt) {\\n              upiAmount = parseFloat(advancePaid);\\n            } else if (isSettlement) {\\n              upiAmount = parseFloat(remainingBalance);\\n            } else {\\n              upiAmount = parseFloat(order.total_price || 0);\\n            }\\n            upiUri = `upi://pay?pa=${activeEntry.upi_id}&pn=${encodeURIComponent(payeeName)}&am=${upiAmount.toFixed(2)}&cu=INR`;\\n          } else {\\n            upiUri = `upi://pay?pa=${activeEntry.upi_id}&pn=${encodeURIComponent(payeeName)}&cu=INR`;\\n          }\\n        }\\n\\n        if (upiUri) {\\n          window.lastUpiUri = upiUri;\\n          const qrDataUrl = await QRCode.toDataURL(upiUri, { margin: 1, width: 140 });\\n          qrCodeHtml = `\\n            <div class=\\\"dashed-line\\\"></div>\\n            <div class=\\\"center\\\" style=\\\"margin: 2mm 0;\\\">\\n               <div style=\\\"font-size: 11.5px; font-weight: bold; margin-bottom: 1mm;\\\">SCAN TO PAY</div>\\n               <img src=\\\"${qrDataUrl}\\\" style=\\\"width: 130px; height: 130px; display: block; margin: 0 auto;\\\" />\\n            </div>\\n          `;\\n        }\\n      } catch (er\n<truncated 81 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating ESC/POS raw HTML print logic\"",
  "toolSummary": "\"Edit App.jsx print logic\""
}