{
  "AllowMultiple": "false",
  "Description": "\"Adjusted billNoDisplay to output 'Quick Bill' when numeric printing is disabled for quick bills.\"",
  "EndLine": "8144",
  "Instruction": "\"Toggle bill number display for Quick Bills in handlePrint\"",
  "ReplacementContent": "\"    const trueGrandTotal = isPreOrder ? (advancePaid + remainingBalance) : totalAmount;\\n    const tableName = order.table_name || (order.table_number ? `Table ${order.table_number}` : (order.table_id ? `Table ${order.table_id}` : 'Takeaway Order'));\\n    const isQuick = activeType === 'QUICK';\\n    const billNoDisplay = (isQuick && !quickBillNumberPrint)\\n      ? 'Quick Bill'\\n      : (order.bill_no || (isPreOrder ? `PO-${order.id}` : order.id));\\n    const paymentMethod = order.payment_method || (isPreOrder && advancePaid > 0 ? 'Advance' : 'Cash');\"",
  "StartLine": "8140",
  "TargetContent": "\"    const trueGrandTotal = isPreOrder ? (advancePaid + remainingBalance) : totalAmount;\\n    const tableName = order.table_name || (order.table_number ? `Table ${order.table_number}` : (order.table_id ? `Table ${order.table_id}` : 'Takeaway Order'));\\n    const billNoDisplay = order.bill_no || (isPreOrder ? `PO-${order.id}` : order.id);\\n    const paymentMethod = order.payment_method || (isPreOrder && advancePaid > 0 ? 'Advance' : 'Cash');\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Toggling bill number display on receipts\"",
  "toolSummary": "\"Modify App.jsx\""
}