{
  "Description": "\"Restore Pickup/Delivery toggle labels and clean up temporary tables when all KOT items are deleted or cancelled\"",
  "Instruction": "\"1. Fix pickup/delivery toggle to show both labels. 2. Fix KOT delete and cancel to remove temp tables when all items are gone.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":7668,\"ReplacementContent\":\"    const activeRemaining = updatedBill.filter(item => !item.isCancelled);\\n    if (activeRemaining.length === 0) {\\n      setTableStatuses(statusPrev => ({ ...statusPrev, [selectedTable.id]: 'AVAILABLE' }));\\n      setTableActiveTimestamps(timePrev => { const n = { ...timePrev }; delete n[selectedTable.id]; return n; });\\n      setTableBillNumbers(numPrev => { const n = { ...numPrev }; delete n[selectedTable.id]; return n; });\\n      releaseTableExtraState(selectedTable.id);\\n      // Remove temporary table from the tables list since it has no active items\\n      if (selectedTable.is_temporary) {\\n        setTables(prev => prev.filter(t => t.id !== selectedTable.id));\\n      }\\n      setIsOldKOTModalOpen(false);\\n      setSelectedTable(null);\\n    }\",\"StartLine\":7660,\"TargetContent\":\"    const activeRemaining = updatedBill.filter(item => !item.isCancelled);\\n    if (activeRemaining.length === 0) {\\n      setTableStatuses(statusPrev => ({ ...statusPrev, [selectedTable.id]: 'AVAILABLE' }));\\n      setTableActiveTimestamps(timePrev => { const n = { ...timePrev }; delete n[selectedTable.id]; return n; });\\n      setTableBillNumbers(numPrev => { const n = { ...numPrev }; delete n[selectedTable.id]; return n; });\\n      releaseTableExtraState(selectedTable.id);\\n      setIsOldKOTModalOpen(false);\\n      setSelectedTable(null);\\n    }\"},{\"AllowMultiple\":false,\"EndLine\":7702,\"ReplacementContent\":\"    const activeItems = updatedBill.filter(item => !item.isCancelled);\\n    if (activeItems.length === 0) {\\n      setTableStatuses(statusPrev => ({ ...statusPrev, [selectedTable.id]: 'AVAILABLE' }));\\n      setTableActiveTimestamps(timePrev => { const n = { ...timePrev }; delete n[selectedTable.id]; return n; });\\n      setTableBillNumbers(numPrev => { const n = { ...numPrev }; delete n[selectedTable.id]; return n; });\\n      releaseTableExtraState(selectedTable.id);\\n      // Remove temporary table from the tables list since all items are cancelled\\n      if (select\n<truncated 3194 bytes>",
  "TargetFile": "\"c:/Users/Sajad/Desktop/SaSLoop/pos-app/src/App.jsx\"",
  "toolAction": "\"Fix toggle labels and temp table cleanup\"",
  "toolSummary": "\"Fix toggle labels and temp table cleanup\""
}