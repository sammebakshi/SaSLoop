{
  "AllowMultiple": "false",
  "Description": "\"Add staffList state, passcode prompt states, and hasAccess / verifyPasscodeAction helpers in App.jsx.\"",
  "EndLine": "3827",
  "Instruction": "\"Add staffList state, passcode prompt states, and access helpers before waitersList state in App.jsx.\"",
  "ReplacementContent": "\"  const [staffList, setStaffList] = useState(() => {\\n    try {\\n      const saved = localStorage.getItem('pos_full_staff');\\n      return saved ? JSON.parse(saved) : [];\\n    } catch (e) { return []; }\\n  });\\n\\n  useEffect(() => {\\n    localStorage.setItem('pos_full_staff', JSON.stringify(staffList));\\n  }, [staffList]);\\n\\n  // Passcode prompt states\\n  const [isPasscodePromptOpen, setIsPasscodePromptOpen] = useState(false);\\n  const [passcodePromptAction, setPasscodePromptAction] = useState('');\\n  const [passcodePromptOnConfirm, setPasscodePromptOnConfirm] = useState(null);\\n  const [passcodePromptPermissionKey, setPasscodePromptPermissionKey] = useState('');\\n\\n  // Access check helpers\\n  const hasAccess = (section, permission) => {\\n    const role = business?.role;\\n    if (!role || role === 'Administrator' || role === 'owner' || role === 'brand_owner' || role === 'master_admin' || String(business?.username || '').toLowerCase().includes('admin')) {\\n      return true;\\n    }\\n    const permissions = getStaffPermissions();\\n    const posAccess = permissions?.pos_access;\\n    if (!posAccess) {\\n      return true; // Safe fallback for older accounts without pos_access config\\n    }\\n    const path = Array.isArray(section) ? [...section] : [section];\\n    if (permission) {\\n      path.push(permission);\\n    }\\n    let current = posAccess;\\n    for (const key of path) {\\n      if (current === undefined || current === null) return false;\\n      current = current[key];\\n    }\\n    return current !== false; // Deny ONLY if explicitly set to false\\n  };\\n\\n  const verifyPasscodeAction = (section, permission, onConfirm, actionName) => {\\n    if (!hasAccess(section, permission)) {\\n      toast.error(`Access Denied: You do not have permission to ${actionName || 'perform this action'}.`);\\n      return;\\n    }\\n    // Check if passcode is required\\n    const passcodeKey = permission ? `${permission}_passcode` : 'visible_passcode';\\n    if (hasAccess(section, passcodeKey) === true) {\\n      setPasscodePromptAction(act\n<truncated 770 bytes>",
  "StartLine": "3827",
  "TargetContent": "\"  const [waitersList, setWaitersList] = useState([]);\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Add access control helpers in App.jsx\"",
  "toolSummary": "\"Add access control helpers\""
}