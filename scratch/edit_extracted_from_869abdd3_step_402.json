{
  "AllowMultiple": "false",
  "Description": "\"Add renderPickupTempTables function after renderPreOrderTempTables to show pickup temporary tables.\"",
  "EndLine": "4604",
  "Instruction": "\"Add renderPickupTempTables function.\"",
  "ReplacementContent": "\"  const renderPickupTempTables = () => {\\n    const pickupTempTables = tables.filter(t => t.is_temporary && t.original_order_type === 'PICKUP');\\n    if (pickupTempTables.length === 0) return null;\\n    return (\\n      <div className={`shrink-0 border-t ${isDark ? 'border-[#30363d] bg-[#0d1117]' : 'border-slate-200 bg-slate-50'}`}>\\n        <div className={`flex items-center justify-between px-3 py-1.5 ${isDark ? 'bg-[#161b22]' : 'bg-[#e6f4ea]'}`}>\\n          <span className={`text-[9px] font-black uppercase tracking-widest ${isDark ? 'text-emerald-400' : 'text-emerald-700'}`}>🛍️ Pickup/Delivery Temporary Tables ({pickupTempTables.length})</span>\\n        </div>\\n        <div className=\\\"flex gap-2 px-3 py-2 overflow-x-auto no-scrollbar\\\">\\n          {pickupTempTables.map(table => {\\n            const billItems = tableBills[table.id] || [];\\n            const activeBillCount = billItems.filter(item => !item.isCancelled).length;\\n            const cartItems = tableCarts[table.id] || [];\\n            const items = billItems.length > 0 ? billItems : cartItems;\\n            const total = calculateTotals(items).total;\\n            let status = tableStatuses[table.id] || 'AVAILABLE';\\n            const hasCartItems = tableCarts[table.id]?.length > 0;\\n            if (activeBillCount > 0 && status === 'AVAILABLE') {\\n                status = 'SAVED';\\n            }\\n            if (activeBillCount === 0 && !['ORDERING', 'RESERVED', 'DRAFT_PRINTED', 'BILL_SAVED', 'PRINTED'].includes(status)) {\\n                status = 'AVAILABLE';\\n            }\\n            if (hasCartItems) {\\n                status = 'ITEMS_IN_KOT';\\n            }\\n            const isSelected = selectedTable?.id === table.id;\\n            const tTime = tableActiveTimestamps[table.id];\\n            const tMin = tTime ? Math.max(1, Math.floor((Date.now() - tTime) / 60000)) : null;\\n            return (\\n              <div\\n                key={table.id}\\n                className={`relative min-w-[140px] max-w-[180px] rounded-xl p-2.5 cursor-poi\n<truncated 4342 bytes>",
  "StartLine": "4599",
  "TargetContent": "\"        </div>\\n      </div>\\n    );\\n  };\\n\\n  const handleKeypadPress = (val) => {\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding renderPickupTempTables function\"",
  "toolSummary": "\"Add renderPickupTempTables function\""
}