{
  "AllowMultiple": "false",
  "Description": "\"Update getFilteredTablesForCurrentView to filter by order modes and custom departments.\"",
  "EndLine": "5191",
  "Instruction": "\"Update table/order list filtering logic based on active department.\"",
  "ReplacementContent": "\"  const getFilteredTablesForCurrentView = () => {\\n    return tables.filter(t => {\\n      // First, filter by the top active department button if selected\\n      if (activeDepartment !== 'All') {\\n        const activeDeptLower = activeDepartment.toLowerCase();\\n        if (activeDeptLower === 'dining') {\\n          const isDineIn = !t.is_temporary || String(t.original_order_type || '').toUpperCase() === 'DINE_IN';\\n          if (!isDineIn) return false;\\n        } else if (activeDeptLower === 'pickup') {\\n          const isPickup = t.is_temporary && String(t.original_order_type || '').toUpperCase() === 'PICKUP' && String(t.original_sub_order_type || '').toUpperCase() !== 'DELIVERY';\\n          if (!isPickup) return false;\\n        } else if (activeDeptLower === 'delivery') {\\n          const isDelivery = t.is_temporary && String(t.original_order_type || '').toUpperCase() === 'PICKUP' && String(t.original_sub_order_type || '').toUpperCase() === 'DELIVERY';\\n          if (!isDelivery) return false;\\n        } else if (activeDeptLower === 'preorder') {\\n          const isPreOrder = t.is_temporary && String(t.original_order_type || '').toUpperCase() === 'PRE_ORDER';\\n          if (!isPreOrder) return false;\\n        } else {\\n          if (t.department_name !== activeDepartment) return false;\\n        }\\n      } else {\\n        // If 'All' is selected, we filter by the current sidebar orderType\\n        if (orderType === 'DINE_IN') {\\n          if (t.is_temporary && String(t.original_order_type || '').toUpperCase() !== 'DINE_IN') return false;\\n        } else if (orderType === 'PICKUP') {\\n          if (t.is_temporary && String(t.original_order_type || '').toUpperCase() !== 'PICKUP') return false;\\n        } else if (orderType === 'PRE_ORDER') {\\n          if (t.is_temporary && String(t.original_order_type || '').toUpperCase() !== 'PRE_ORDER') return false;\\n        } else {\\n          if (t.is_temporary) return false;\\n        }\\n      }\\n\\n      if (tableSearchQuery) {\\n        if (!t.table_name.toLowerCase().incl\n<truncated 96 bytes>",
  "StartLine": "5172",
  "TargetContent": "\"  const getFilteredTablesForCurrentView = () => {\\n    return tables.filter(t => {\\n      if (orderType === 'DINE_IN') {\\n        if (t.is_temporary && t.original_order_type !== 'DINE_IN') return false;\\n      } else if (orderType === 'PICKUP') {\\n        if (t.is_temporary && t.original_order_type !== 'PICKUP') return false;\\n      } else if (orderType === 'PRE_ORDER') {\\n        if (t.is_temporary && t.original_order_type !== 'PRE_ORDER') return false;\\n      } else {\\n        if (t.is_temporary) return false;\\n      }\\n      if (orderType === 'DINE_IN' && activeDepartment !== 'All') {\\n        if (t.department_name !== activeDepartment) return false;\\n      }\\n      if (tableSearchQuery) {\\n        if (!t.table_name.toLowerCase().includes(tableSearchQuery.toLowerCase())) return false;\\n      }\\n      return true;\\n    });\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating table filtering function in App.jsx\"",
  "toolSummary": "\"Updating table filtering function in App.jsx\""
}