{
  "AllowMultiple": "false",
  "Description": "\"Update calculateTotals in App.jsx to calculate dynamic discount and additional charges\"",
  "EndLine": "1264",
  "Instruction": "\"Update calculateTotals to incorporate appliedAdditionalCharges in the extraFixed calculation\"",
  "ReplacementContent": "\"  const calculateTotals = (activeCart = cart) => {\\n    const subtotal = activeCart.reduce((acc, item) => {\\n      const itemBase = (item.price || 0) * item.quantity;\\n      const modifiersTotal = (item.modifiers || []).reduce((mAcc, m) => mAcc + (parseFloat(m.price) || 0) * item.quantity, 0);\\n      return acc + itemBase + modifiersTotal;\\n    }, 0);\\n\\n    let discountAmt = 0;\\n    if (discount.type === 'PERCENT' || discount.type === 'percent') {\\n      discountAmt = subtotal * (parseFloat(discount.value || 0) / 100);\\n    } else if (discount.type === 'FIXED' || discount.type === 'fixed') {\\n      discountAmt = parseFloat(discount.value || 0);\\n    }\\n\\n    const amountAfterDiscount = Math.max(0, subtotal - discountAmt);\\n    const taxRate = parseFloat(posSettings.taxRate || 0);\\n    const isInclusive = posSettings.isTaxInclusive;\\n    \\n    let tax = 0;\\n    let taxableAmount = amountAfterDiscount;\\n    \\n    if (isInclusive) {\\n      tax = amountAfterDiscount * (taxRate / (100 + taxRate)); // Extract tax\\n      taxableAmount = amountAfterDiscount - tax;\\n    } else {\\n      tax = amountAfterDiscount * (taxRate / 100); // Calculate tax on top\\n    }\\n    \\n    const cgst = tax / 2;\\n    const sgst = tax / 2;\\n    \\n    const serviceCharge = orderType === 'DINE_IN' && posSettings.enableServiceCharge \\n      ? (taxableAmount * (parseFloat(posSettings.serviceChargeRate || 5) / 100)) \\n      : 0;\\n    \\n    let additionalChargesAmt = 0;\\n    appliedAdditionalCharges.forEach(chg => {\\n      const val = parseFloat(chg.value || 0);\\n      if (chg.type === 'percent' || chg.type === 'PERCENT') {\\n        additionalChargesAmt += amountAfterDiscount * (val / 100);\\n      } else {\\n        additionalChargesAmt += val;\\n      }\\n    });\\n\\n    const extraFixed = parseFloat(extraCharges.packing || 0) + parseFloat(extraCharges.delivery || 0) + additionalChargesAmt;\\n    const total = isInclusive ? amountAfterDiscount + serviceCharge + extraFixed : taxableAmount + tax + serviceCharge + extraFixed;\\n\\n    return { \\n      sub\n<truncated 139 bytes>",
  "StartLine": "1220",
  "TargetContent": "\"  const calculateTotals = (activeCart = cart) => {\\n    const subtotal = activeCart.reduce((acc, item) => {\\n      const itemBase = (item.price || 0) * item.quantity;\\n      const modifiersTotal = (item.modifiers || []).reduce((mAcc, m) => mAcc + (parseFloat(m.price) || 0) * item.quantity, 0);\\n      return acc + itemBase + modifiersTotal;\\n    }, 0);\\n\\n    let discountAmt = 0;\\n    if (discount.type === 'PERCENT') discountAmt = subtotal * (parseFloat(discount.value || 0) / 100);\\n    else if (discount.type === 'FIXED') discountAmt = parseFloat(discount.value || 0);\\n\\n    const amountAfterDiscount = subtotal - discountAmt;\\n    const taxRate = parseFloat(posSettings.taxRate || 0);\\n    const isInclusive = posSettings.isTaxInclusive;\\n    \\n    let tax = 0;\\n    let taxableAmount = amountAfterDiscount;\\n    \\n    if (isInclusive) {\\n      tax = amountAfterDiscount * (taxRate / (100 + taxRate)); // Extract tax\\n      taxableAmount = amountAfterDiscount - tax;\\n    } else {\\n      tax = amountAfterDiscount * (taxRate / 100); // Calculate tax on top\\n    }\\n    \\n    const cgst = tax / 2;\\n    const sgst = tax / 2;\\n    \\n    const serviceCharge = orderType === 'DINE_IN' && posSettings.enableServiceCharge \\n      ? (taxableAmount * (parseFloat(posSettings.serviceChargeRate || 5) / 100)) \\n      : 0;\\n    \\n    const extraFixed = parseFloat(extraCharges.packing || 0) + parseFloat(extraCharges.delivery || 0);\\n    const total = isInclusive ? amountAfterDiscount + serviceCharge + extraFixed : taxableAmount + tax + serviceCharge + extraFixed;\\n\\n    return { \\n      subtotal, \\n      tax, \\n      cgst, \\n      sgst, \\n      serviceCharge, \\n      total, \\n      extraFixed,\\n      discountAmt\\n    };\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Updating calculateTotals in App.jsx\"",
  "toolSummary": "\"Update calculateTotals\""
}