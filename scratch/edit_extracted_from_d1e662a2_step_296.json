{
  "AllowMultiple": "false",
  "Description": "\"Update checkBillingPermission, checkOldKOTPermission, and checkKOTPermission helpers to check bypassedPermissionsRef\"",
  "EndLine": "3771",
  "Instruction": "\"Integrate bypassedPermissionsRef check at the beginning of checkBillingPermission, checkOldKOTPermission, and checkKOTPermission in App.jsx\"",
  "ReplacementContent": "\"  const checkBillingPermission = (perm) => {\\n    if (bypassedPermissionsRef.current.has(perm)) return true;\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    if (orderType === 'QUICK') {\\n      const qk = perm === 'add_charges' ? 'add_charge' : perm;\\n      return access.QuickBill?.[qk] !== false;\\n    }\\n    let billingAccess = access.Billing;\\n    if (orderType === 'DELIVERY') {\\n      billingAccess = access.Delivery?.Billing;\\n    } else if (orderType === 'PICKUP') {\\n      billingAccess = access.Pickup?.Billing;\\n    } else if (orderType === 'PRE_ORDER') {\\n      billingAccess = access.PreOrder?.Billing;\\n    }\\n    return billingAccess?.[perm] !== false;\\n  };\\n\\n  const checkOldKOTPermission = (perm) => {\\n    if (bypassedPermissionsRef.current.has(perm)) return true;\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    let oldKotAccess = access.OldKOT;\\n    if (orderType === 'DELIVERY') {\\n      oldKotAccess = access.Delivery?.OldKOT;\\n    } else if (orderType === 'PICKUP') {\\n      oldKotAccess = access.Pickup?.OldKOT;\\n    } else if (orderType === 'PRE_ORDER') {\\n      oldKotAccess = access.PreOrder?.OldKOT;\\n    }\\n    return oldKotAccess?.[perm] !== false;\\n  };\\n\\n  const checkKOTPermission = (perm) => {\\n    if (bypassedPermissionsRef.current.has(perm)) return true;\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    return access.KOT?.[perm] !== false;\\n  };\"",
  "StartLine": "3734",
  "TargetContent": "\"  const checkBillingPermission = (perm) => {\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    if (orderType === 'QUICK') {\\n      const qk = perm === 'add_charges' ? 'add_charge' : perm;\\n      return access.QuickBill?.[qk] !== false;\\n    }\\n    let billingAccess = access.Billing;\\n    if (orderType === 'DELIVERY') {\\n      billingAccess = access.Delivery?.Billing;\\n    } else if (orderType === 'PICKUP') {\\n      billingAccess = access.Pickup?.Billing;\\n    } else if (orderType === 'PRE_ORDER') {\\n      billingAccess = access.PreOrder?.Billing;\\n    }\\n    return billingAccess?.[perm] !== false;\\n  };\\n\\n  const checkOldKOTPermission = (perm) => {\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    let oldKotAccess = access.OldKOT;\\n    if (orderType === 'DELIVERY') {\\n      oldKotAccess = access.Delivery?.OldKOT;\\n    } else if (orderType === 'PICKUP') {\\n      oldKotAccess = access.Pickup?.OldKOT;\\n    } else if (orderType === 'PRE_ORDER') {\\n      oldKotAccess = access.PreOrder?.OldKOT;\\n    }\\n    return oldKotAccess?.[perm] !== false;\\n  };\\n\\n  const checkKOTPermission = (perm) => {\\n    const access = getStaffPermissions()?.pos_access;\\n    if (!access) return true;\\n    return access.KOT?.[perm] !== false;\\n  };\"",
  "TargetFile": "\"c:\\\\Users\\\\Sajad\\\\Desktop\\\\SaSLoop\\\\pos-app\\\\src\\\\App.jsx\"",
  "toolAction": "\"Adding bypass check to check permission helper functions\"",
  "toolSummary": "\"Adding bypass check to check permission helper functions\""
}